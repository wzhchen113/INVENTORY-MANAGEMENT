## Verdict
verdict: SHIP_READY
rationale: All four reviewers cleared with zero Criticals, both code-reviewer Should-fixes were folded in and re-verified, and the latest test.yml on main is green.

## Findings summary
- code-reviewer: 0 Critical / 2 Should-fix / 5 Nits. Both Should-fixes RESOLVED in the post-review fix-pass (see the "## Resolution" section in code-reviewer.md): S1 (`eodQueue.ts` idempotency-guard comment contradicting the `'[]'` fall-through) reworded to match the intentional, idempotent behavior — comment-only, no code change; S2 (`EODCount.tsx` live-total `cp`/`up` → `casesParsed`/`unitsParsed`) renamed to match the submit path — local rename, no behavior change. Re-verified post-fix: `jest src/screens/staff/screens/EODCount src/screens/staff/lib/eodQueue` 36/36 green, base `tsc --noEmit` exit 0. The 5 Nits remain deferred (cosmetic — stale-v1-key cleanup, pgTAP fixture null-guard, `V1EodEntry` cast, `caseQty=1` label suppression, GRANT comment length).
- security-auditor: 0 across every severity (Critical/High/Medium/Low). SECURITY DEFINER `auth_can_see_store(p_store_id)` gate preserved byte-for-byte; `v_actor` still `auth.uid()`-wins (no spoofing); 7-arg signature unchanged so the `authenticated`-only GRANT + `public/anon/service_role` REVOKE survive `create or replace` (pinned by the pgTAP `has_function_privilege` assertion); the two client numeric values are read via `jsonb_to_recordset ... numeric` (no SQLi surface) and only land in two nullable `eod_entries` columns (never `current_stock`, audit string, RPC return, or any report); backward-compat path (absent keys → NULL) runs the identical gate + triggers (not a bypass); the device-local `:v1 → :v2` migrate cannot forge actor/store (drain skips foreign `intent_user_id`, server re-derives the actor and re-checks store membership).
- test-engineer: 20/20 ACs PASS, no coverage gaps. Added the missing AC-SCREEN-4 "cases-only" predicate test (jest 459 → 460, 47 suites). Track 2 pgTAP 41/41 incl. the new `staff_submit_eod_cases_each.test.sql` 6/6 (GRANT survived, three values persist, legacy entry without split keys still inserts, total stored as-received, per-store 42501 gate intact, no `set role anon` probe). All 3 typechecks (base / test-graph / e2e) exit 0. Track 4 (Playwright) NOT run locally — fixture `:v2` flips verified by inspection, e2e typecheck passes; advisory-in-CI per posture, non-blocking. Vacuousness audit on the critical ACs passes (each would fail if its formula/INSERT/migrate logic were broken).
- backend-architect: MATCHES DESIGN, 0 Critical / 0 Should-fix / 2 Minor (both pre-flagged optional in the spec). RPC migration is exactly the two-hunk `create or replace` specified (signature byte-identical, GRANT preserved and not re-emitted, backward-compatible, `current_stock`/audit-log still writing the total, no RLS change, no realtime publication change); PIN 1 reads `case_qty` from `catalog_ingredients` with the null-preserving map; PIN 2's `EodEntry` is the 3-field shape with `count` removed (not aliased) and the read-once `:v1→:v2` migrate is idempotent + crash-safe (write-v2-then-remove-v1, wired through `App.tsx migrateQueueIfNeeded() → hydrateQueue()`); convert formula byte-identical to the admin worksheet; reports untouched; scope staff-only with no `db.ts` bypass. M1 = stale `EODCountSection.tsx:60` admin comment; M2 = cosmetic `Number.isNaN` vs admin's `isNaN` (behaviorally identical on the `parseFloat` operands).

## Recommended next steps (ordered)
1. **Commit the spec-086 change set** (UNSTAGED, awaiting the user's explicit commit gate). The commit covers: the new migration `supabase/migrations/20260601000000_staff_submit_eod_cases_each.sql` + new pgTAP `supabase/tests/staff_submit_eod_cases_each.test.sql`; the staff slice (`src/screens/staff/lib/types.ts`, `screens/EODCount.tsx`, `hooks/useEodSubmit.ts`, `lib/eodQueue.ts`, `i18n/en.json`, plus the affected jest specs and `store/useStaffStore.test.ts`); the e2e/docs `:v2` fixture flips (`e2e/fixtures/constants.ts`, `e2e/eod.spec.ts`, `e2e/auth.setup.ts`, `tests/README.md`); and `specs/086/`.
2. **After merge, apply the migration to prod:** the user runs `npx supabase db push --linked` to apply `20260601000000_staff_submit_eod_cases_each.sql`. Until then the `db-migrations-applied` drift gate will flag one unapplied local migration — expected. The RPC change is additive + backward-compatible (absent split keys → NULL), so old staff clients keep working during rollout.
3. **No action needed for the staff-device queue migration:** the `:v1 → :v2` read-once-migrate runs automatically on each staff device's next app load (crash-safe write-then-remove ordering); any in-flight queued counts are preserved as units-only legacy entries. No user step.
4. **Confirm CI is green post-merge:** per CLAUDE.md policy, verify the most recent `test.yml` run on `main` is green via `gh run list --branch main --limit 1` after the push lands. (Pre-merge baseline is green — run 26734770858, head `78fa1ab` — so the "no SHIP_READY on red main" hard rule is satisfied at proposal time.)

### Optional follow-ups (NOT blocking ship)
- The 5 code-reviewer Nits (stale-v1-key one-time cleanup if support reports show accumulation; pgTAP fixture null-guard for future seed refreshes; drop the redundant `Partial<V1EodEntry>` cast; the `caseQty = 1` label-suppression UX call for the PM; the GRANT comment-length asymmetry).
- The architect's 2 Minors: the stale `EODCountSection.tsx:60` admin comment ("Single qty input per item …") and the cosmetic `Number.isNaN` vs `isNaN` (the staff form is arguably the stricter/correct one — no change required).
- A future Track-4 e2e that fills BOTH Cases and Units and asserts both pre-fill on reload (today the e2e fills Units only).

## Out of scope for this review
- A pack-size-aware variance/reorder report that reads `_cases`/`_each` directly — explicitly a future spec; the OQ-1 convert decision means zero report work here.
- A units-per-case management/data-entry UI — staff reads the existing `catalog_ingredients.case_qty` read-only.
- The human-readable cases+units breakdown in the audit-log value — explicitly out of v1 (OQ-3); the RPC keeps writing the total + unit.
- Any admin EOD worksheet change — the admin already has the two boxes (OQ-6); `EODCountSection.tsx` is read-only reference.
