## Verdict
verdict: SHIP_READY
rationale: All four reviewers are Critical-free with the sole Should-fix resolved by a verified artifact, the three load-bearing safety properties independently confirmed, and both CI gates green on `main` before the (expected) post-push drift-gate red — which resolves on the required prod apply.

## Findings summary
- **code-reviewer:** 0 Critical, 1 Should-fix (RESOLVED — verified), 4 nits (all out-of-scope / pre-existing). The Should-fix (RunImportModal pre/post count mismatch on idempotent re-import) is closed with a real artifact: the `codePreview.toWrite` loop now compares the resolved code against the existing link's trimmed `orderCode` and only counts a change (`RunImportModal.tsx:83-90`, `existingCode !== op.orderCode`), byte-mirroring `commitImport`'s promotion rule. Option (a) — the honest count — was chosen over relabeling. Nits are pre-existing `#000`-on-accent literals + hardcoded English in untouched summary segments + a design-doc-vs-code signature narrowing (correct simplification) + the section-level test-coverage question (deferred to test-engineer, dispositioned below). None introduced by 115.
- **security-auditor:** 0 Critical / 0 High / 0 Medium / 0 Low. Verified LIVE against the running local stack: (1) the W-2 `order_unit` gate — non-privileged 2222 `user` `UPDATE … SET order_unit='unit'` affects `UPDATE 0` rows, value stays `case`; privileged 1111 admin gets `UPDATE 1`; the CHECK rejects `'pallet'` with `23514`. (2) W-1 CSV write is parameterized PostgREST (no SQLi — grepped every changed file for dynamic-SQL sinks), renders operator input only through RN `<Text>`/clipboard plain text (no HTML sink), and is RLS-gated by the transitive `store_member_*_item_vendors` policies (a CSV cannot cross stores; vendor resolution is bounded to the RLS-scoped `vendors` slice and never auto-creates a vendor). (3) Additive DDL only — no DROP/GRANT/REVOKE/ALTER PUBLICATION/CREATE POLICY. No edge function, no secrets.
- **test-engineer:** 0 FAIL / 0 NOT TESTED. 17 of 20 lettered ACs unconditional PASS; 5 (AC-12/13/15/16/17) PASS on combined byte-for-byte builder/RLS jest + main Claude's live browser evidence. Both coverage-depth gaps dispositioned in its Resolution: AC-5/6 (RunImportModal count-surfacing UI) — the underlying `commitImport` counts ARE jest-pinned and the Should-fix aligned the preview count to that pinned path; a dedicated component test is a backlog follow-up. AC-12/13/15/16/17 (POsSection roundedCount toast + the whole new ReorderSection button caller layer) — graded PASS on live browser evidence + source review; the shared builder they call is byte-for-byte jest-pinned. `vendors_role_access.test.sql` extended 4→11 assertions (design sketch estimated 7; landed finer at 11), all passing.
- **backend-architect (post-impl):** 0 Critical / 0 Should-fix / 0 Minor — WITHIN DESIGN, no drift. All 7 requested checks landed as written, including the two overturned FLAGGED premises (OQ-3: `public.vendors` already in `supabase_realtime` since `20260514140000` → no publication change, no docker-restart step; OQ-4: `privileged_update_vendors` already gates UPDATE column-agnostically → no new policy, stale `20260517010000` comment made honest by the pgTAP) and the #1 flagged drift check (the W-1 reconcile-delete trap is closed by construction — CSV write feeds the full existing link set, `updateInventoryItem`'s reconcile byte-identical to the spec-114 baseline).

### Three load-bearing safety properties — all independently confirmed
1. **W-1 reconcile-safety guard (genuine cost-preserving pin, not shape-only).** `buildOrderCodeVendorsPayload` (`csvImport.ts:198-213`) preserves each existing link's `costPerUnit`/`casePrice` verbatim and overwrites `orderCode` only on the resolved link. The jest pin (`csvImport.test.ts` — 3-vendor item, code written to A only) asserts the FULL submitted payload array with literal cost numbers via `toEqual`, so it catches a bug that kept vendorIds but zeroed B/C costs. Verified by code-reviewer, test-engineer, AND backend-architect independently.
2. **W-2 order_unit privileged-gate.** security-auditor proved LIVE that a non-privileged `user` UPDATE = `UPDATE 0` rows (value unchanged), privileged admin = `UPDATE 1`; `order_unit` inherits `privileged_update_vendors` column-agnostically with no new policy.
3. **Additive-only migration.** `20260709000000_vendor_order_unit.sql` is exactly `ALTER TABLE … ADD COLUMN IF NOT EXISTS order_unit text NOT NULL DEFAULT 'case' CHECK (order_unit IN ('case','unit'))` + a `COMMENT` — metadata-only on PG17, reversible-by-design, no policy/grant/publication hunk (confirmed by security-auditor grep + architect).

No unreasonable waiver was taken: every "PASS with residual gap" is data-layer-proven-plus-browser-confirmed, not absent coverage; every nit is pre-existing or a correct simplification.

## Recommended next steps (ordered)

SHIP_READY. Two user-gated ship steps (backend surface + additive DDL column) — the developer flags the prod apply; they do not push it. Neither is a blocker to the SHIP_READY verdict; both are the standard release mechanics for a spec that adds a column.

1. **Commit + push to `main`.** Then confirm both active gates per the CLAUDE.md CI-status rule: `test.yml` must be green (jest 1096/1096 across 95 suites; test:db 64/64; both typechecks exit 0 — all verified locally by main Claude). **`db-migrations-applied.yml` will go RED after the push** because `20260709000000` is in the repo but not yet in prod's `schema_migrations` — this is EXPECTED and is resolved by step 2, not a drift regression. (Verified: `vendors_role_access.test.sql` reports 11/11 assertions; no `.only`/`.skip` in the four spec-115 files.)
2. **Prod-apply `20260709000000_vendor_order_unit.sql` via Supabase MCP** (project `ebwnovzzkwhsdxkpyjka`; `db push` lacks the prod password): `execute_sql` the `ALTER TABLE … ADD COLUMN order_unit text NOT NULL DEFAULT 'case' CHECK (…)` + the `COMMENT ON COLUMN` (metadata-only on PG17), `INSERT` version `20260709000000` into `supabase_migrations.schema_migrations`, then VERIFY by COLUMN PRESENCE (DDL, not body-md5 — there is no function here). This re-greens `db-migrations-applied.yml`.
3. **(optional follow-ups, not blocking ship — cleanup backlog):** a `RunImportModal.test.tsx` render test asserting the toast-tail counts + `import-code-preview` skip-reason rows (test-engineer's AC-5/6 note); extend `POsSection.test.tsx` with a fractional-caseQty fixture and add a minimal `ReorderSection` quick-order-button test mirroring the PO suite (AC-12/15/16/17 caller-layer coverage). Both convert browser-plus-inference PASSes to fully automated; neither gates this ship.

## Out of scope for this review
- **PRE-EXISTING POSImportsSection infinite-render loop** (previewOverrides effect) — byte-identical at 806c6d9, unrelated to spec 115's `computeDiff`-arg change; a separate follow-up, NOT introduced by this spec and explicitly NOT a SHIP_READY blocker (confirmed by main Claude, backend-architect, and test-engineer).
- **Pre-existing vendor-RLS/editor gaps** surfaced by backend-architect for a future spec (do NOT fix here): (a) the second INSERT policy `"Vendors admin only"` lacks the `auth_can_see_brand` clause → a privileged brand-A user could INSERT a vendor for brand B via the ORed permissive policy; (b) `db.ts updateVendor` still drops `deliveryDays`/`categories` on update (in `toUpdates()` but not threaded into `dbUpdates`) — the `order_unit` thread added its own explicit line and did NOT widen this.
- **Pre-existing hardcoded-English summary segments + `#000`-on-accent literals** in `RunImportModal.tsx` / `VendorFormDrawer.tsx` — untouched by 115 (spec only appended the three new localized `T('section.posImports.*')` calls); matches the tracked `#000` cleanup-backlog item. Separate spec.
