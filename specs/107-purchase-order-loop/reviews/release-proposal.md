## Verdict
verdict: SHIP_READY
rationale: Zero Critical across all four reviewers, the single Should-fix (Commit Receive confirm gate) is fixed and re-verified (jest 864/864, both typechecks 0), and all 14 acceptance criteria PASS.

## Findings summary
- code-reviewer: 0 Critical, 1 Should-fix (RESOLVED), 4 Nits. Should-fix was the ReceivingSection "Commit Receive" confirm-gating asymmetry — the only stock-mutating lifecycle action without a `confirmAction` gate while its four siblings had one. FIXED post-review: `onCommit` now wraps the RPC in `confirmAction` (title/body/CTA i18n'd across all three locales, `section.receiving.commitConfirm*`), with jest asserting the confirm fires exactly once before the RPC and is never opened on the zero-lines path. Nits (all advisory, left as noted): duplicated 4-line `PoRow` type alias across POsSection/ReceivingSection; an extra `loadPurchaseOrderLines` round-trip in `receivePurchaseOrder` the RPC envelope could satisfy; a pre-existing `'submitted'` jest fixture (out of this spec's changed-files scope); a pre-existing status-unaware RLS shape on `po_items` UPDATE/DELETE (architecture territory, already signed off by backend-architect §0). Author also confirmed the full golden chain live in the browser (create -> send -> partial receive -> feedback -> cancel), documented in the file's Browser-verification appendix.
- security-auditor: 0 Critical, 0 Should-fix, 3 Nits. PASS. Store isolation enforced at three layers (RLS on tables, INVOKER RLS on the RPC UPDATEs, explicit `auth_can_see_store` gate + store-id pin); edge fn `send-po-email` re-reads authoritatively keyed only on `poId`, escapes every interpolated HTML value via the byte-identical five-char `escapeHtml`, honors the `ADMIN_ROLES` (incl. `super_admin`) 10th-omission parity rule, flips status server-side only on a Resend 2xx guarded `.eq('status','draft')`, and sources both secrets from `Deno.env` with zero `console.*`. FE routes the send through `callEdgeFunction` (not raw fetch); all outward/destructive actions confirm-gated; receive `client_uuid` minted with `crypto.randomUUID()`. Nits (non-blocking robustness/doc-parity): unbounded `received_qty` (no cross-tenant reach — RLS/store-pinned, and a member already has raw write to their own `current_stock`); the global-vs-per-PO idempotency index asymmetry (self-inflicts a 23505 at worst, no cross-write); Resend/PostgREST error strings surfaced to the client (no row data / stack / cross-store leak).
- test-engineer: 14/14 ACs PASS (no FAIL). pgTAP 61/61 files incl. `po_loop.test.sql` 30/30; jest 80 suites / 864 tests green; both typechecks exit 0; no test modified or worked around. Three sub-component NOT-TESTED flags surfaced per convention: (1) FE `pendingPoQty` rendering in the reorder "inbound" segment — data correctness is pgTAP-proven and the wiring is a straight one-line pass-through; SINCE CLOSED by the author's live browser pass (the INBOUND column renders and a fully-covered vendor drops off the reorder list); (2) `send-po-email` edge fn HTTP behavior — no automated coverage (Deno edge fns unreachable from jest), spec-acknowledged as optional, code-read-verified by two reviewers (security-auditor + test-engineer + architect); (3) realtime leg of AC14 — standing repo-wide infra gap (no WS harness), also covered belt-and-suspenders by a deterministic `refreshPurchaseOrders()` + `loadReorderSuggestions()` after every lifecycle mutation. Also independently confirmed both build-time design-vs-code deviations (idempotency coalesce direction; P0002-vs-42501).
- backend-architect: 0 Critical, 0 Should-fix, 4 Minor (all non-blocking). Implementation matches the design. All three flagged build-time deviations ruled CORRECT — keep as implemented: (1) idempotency last-key-wins `coalesce(p_client_uuid, receive_client_uuid)` (the design's own prose intent; first-key-wins would double-increment a completing receive's retry); (2) P0002 for a non-member under INVOKER+RLS is the code's real behavior and a strictly stronger existence-hiding posture than 42501 — §10(e) AC text reconciled TO P0002 (no code change); (3) legacy `createPurchaseOrder` -> 'sent' (NOT 'draft') is required so the already-ordered quantity leaves `pending_po_qty`. Both reorder engines copied their own latest bodies with only the CTE swapped and the two `pending_po_qty` CTEs are byte-identical; no publication change; edge fn honors every §6/§7 decision. Minor notes (M1 global idempotency index scope, M2 Receiving raw-status filter vs POsSection normalizeStatus, M3 draft `total_cost` is display-derived, M4 legacy header-only PO contributes 0 to pending) are all pre-existing or design-intended and flagged only for the record.

## Recommended next steps (ordered)
SHIP_READY. Note this proposal is a PRE-PUSH assessment: the code + local suites are ship-clean, so the ship itself is authorized, but the prod-facing steps below are USER-GATED (per the spec) and there are three of them this spec, not two. The SHIP_READY verdict stands on the code/test evidence; the CI-green hard rule re-engages at step 5. Order:

1. User authorizes commit and push to `main`. Expected transient: `test.yml` + e2e stay green; `db-migrations-applied.yml` goes RED between push and step 2 because migration `20260704000000_po_loop.sql` is committed but not yet in prod's `schema_migrations` — this is the same expected transient as specs 104/105/106, NOT a regression. Do not treat that specific red as a blocker; it clears at step 2.
2. Prod SQL apply via Supabase MCP: apply `20260704000000_po_loop.sql` (status normalization touches prod's 6 PO rows `'submitted'` -> `'sent'`; additive columns/indexes incl. `receive_client_uuid` + partial unique index; 3 new lifecycle RPCs; both reorder RPC re-CREATEs) THEN insert the exact `20260704000000` version into `supabase_migrations.schema_migrations`. Verify the three new RPCs + both re-CREATEd reorder RPCs with a normalized-md5 check per the project's prod-migration playbook.
3. NEW this spec — deploy the `send-po-email` edge function to prod (supabase CLI or MCP deploy). Without the deploy, "Send to vendor" fails gracefully and Mark-as-sent-manually remains the working path — so this step is required for the email leg but does not block the rest of the loop.
4. Confirm the `RESEND` secret exists in prod env before relying on step 3. Assumption to VERIFY, not assume: it is present for `send-invite-email`, but confirm the exact env-var name the function reads (`RESEND_API_KEY` per the audited source) is set in prod. If absent, the send returns 502 and the manual path still works — surface it rather than shipping a silent-failure send button.
5. Confirm BOTH CI gates are green on `main`: latest `test.yml` run AND latest `db-migrations-applied.yml` run (the latter should flip back to green once step 2 lands the migration in `schema_migrations`). If either is red or in-progress after step 2, surface the run URL and wait for user direction before closing out.

Optional follow-ups (not blocking ship):
- Sweep the pre-existing `db.crossStoreLoaders.test.ts` `'submitted'` fixtures to `'sent'` next time that file is touched (code-reviewer Nit; `'submitted'` is now a value the live CHECK constraint can never produce).
- Add a jest render-assertion for a non-zero `pendingPoQty` in the reorder "inbound" segment to convert the live-browser verification into standing regression coverage (test-engineer gap #1).
- Consider a one-line comment on the global `receive_client_uuid` unique index documenting the global-index / per-PO-read asymmetry (security-auditor + architect M1).

## Out of scope for this review
- Status-scoped RLS on `po_items` UPDATE/DELETE (line edits are FE-gated to draft-only; the backing policy is store-scoped, not status-scoped). Pre-existing RLS shape, explicitly signed off in backend-architect design §0, flagged by code-reviewer only for visibility — belongs in a dedicated RLS-tightening spec if the owner wants it, not spec 107.
- Backfilling `po_items` lines onto the legacy header-only `createPurchaseOrder` path so it also suppresses reorder (backend-architect M4). Pre-existing limitation the spec deliberately did not scope in; a future follow-up spec.
- `send-po-email` automated (shell-smoke / integration) coverage. The spec explicitly marks shell smoke for this surface as optional and names no required track for the email-send path; standing infra gap (Deno edge fns unreachable from jest). A future test-infra spec, not a spec-107 blocker.
- Realtime-leg direct test coverage (AC14). Repo-wide standing gap (no WS harness), identical to already-shipped specs 087/102/104/105; a future test-infra spec.
