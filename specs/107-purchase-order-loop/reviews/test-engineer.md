## Test report for spec 107

### Acceptance criteria status

Backend / data:

- AC1 (`receive_purchase_order(p_po_id, p_lines, p_client_uuid)` writes `po_items.received_qty`, increments `current_stock`, flips status `received`/`partial`, stamps `received_at`/`received_by`, writes one audit row, returns id+status+per-line totals) → **PASS** — `supabase/tests/po_loop.test.sql::(A)/(B)/(D)` (full receive, short receive, accumulate-to-complete). Audit-row-write is exercised implicitly (the RPC body always inserts one `audit_log` row per call and no arm fails) but not directly asserted with a `count(*) from audit_log` check — a minor gap, not a BLOCK, since the RPC path is unconditional and every receive arm succeeds.
- AC2 (receiving is stock-only — no cost mutation) → **PASS** — `po_loop.test.sql::(E)` asserts both `inventory_items.cost_per_unit` and `item_vendors.case_price` are byte-identical before/after a receive.
- AC3 (short receive → `partial`, `received_at` NULL, remainder stays inbound; separate "close short" releases it) → **PASS** — `po_loop.test.sql::(B)` (partial + NULL `received_at`) and `(G)` (`close_short_purchase_order` transitions `partial → received`, stamps `received_at`, refuses non-`partial` with P0001).
- AC4 (idempotent on `p_client_uuid`; replay returns first result, no double-increment) → **PASS** — `po_loop.test.sql::(C)`. Note: a real design-vs-code contradiction was caught and fixed during build — the design's §3 snippet had `coalesce(receive_client_uuid, p_client_uuid)` (keeps the FIRST key, would NOT dedupe a second sequential partial's retry), corrected in the shipped migration to `coalesce(p_client_uuid, receive_client_uuid)` (new-key-wins). Verified the fix landed at `supabase/migrations/20260704000000_po_loop.sql:269` and is exercised by the two-partial-then-replay sequence in `(C)/(D)`.
- AC5 (`SECURITY INVOKER`, `search_path=public`, refuses when `auth_can_see_store` is false) → **PASS** — `po_loop.test.sql::(F)`. Note: a second design-vs-code discrepancy is flagged (not a defect, an intentional resolution) — under INVOKER + RLS a non-member's SELECT on the auth-gate query returns 0 rows, so the RPC raises `P0002` ('not found') before it can reach the `42501` branch documented in the spec's §10(e) wording. The shipped code's behavior (P0002, which also avoids leaking PO existence) is what pgTAP pins; the `42501` code path remains dead for a real non-member caller and only fires in the theoretical row-visible-but-`auth_can_see_store`-disagrees case. Functionally this satisfies "refuses when not authorized" — flagging for the release-coordinator/architect to decide if the AC wording needs reconciling to P0002, since it is a real code-vs-doc divergence even though the shipped behavior is arguably stronger.
- AC6 (`pending_po_qty` CTE swapped from `0::numeric` to a real per-item open-PO aggregate in BOTH engines, per-item/`select distinct`-safe) → **PASS** — `po_loop.test.sql::(P)` seeds an open `sent` PO on an item and asserts non-zero `pending_po_qty` + reduced `suggested_qty` from `report_reorder_list`. Manually diffed both re-CREATE'd CTEs in `20260704000000_po_loop.sql` (lines ~659-684 and ~1251-1276) — byte-identical comment block and SQL body confirmed by direct read, not just by test pass.
- AC7 (two engine RPCs remain byte-identical in the CTE + downstream use; pgTAP asserts non-zero `pending_po_qty` + reduced `suggested_qty` from BOTH) → **PASS** — `po_loop.test.sql::(P)` runs both `report_reorder_list` and `report_reorder_for_counted_onhand` against the same fixture and asserts parity: `48 == 140 - 92` (vendor-engine's reported pending == counted-engine's suggestion reduction). Pre-existing suites `report_reorder_list_multi_vendor.test.sql` and `report_reorder_for_counted_onhand.test.sql` (which predate spec 107 and never reference `pending_po_qty`) both still pass, confirming the re-CREATEs didn't regress specs 087/088/100/102/104/105.
- AC8 (one-click PO from a reorder vendor card creates an editable DRAFT: header `status='draft'` + one `po_items` row per suggested line, `ordered_qty` prefilled, `cost_per_unit` per OQ-6) → **PASS** — jest `src/lib/db.poLoop.test.ts::createPurchaseOrderDraft` pins the header-then-lines payload shape incl. `status:'draft'` and the per-line `cost_per_unit` snapshot; `useStore.ts::createPoDraft` (read directly) builds lines from `vendor.items` using `suggestedUnits` and the `costPerUnit × subUnitSize` bridge read from the `inventory` array by `itemId`, matching the spec-104 ★ bridge exactly. `ReorderSection.tsx::CreatePoButton` wires this behind `confirmAction`.
- AC9 (`po_items.cost_per_unit` snapshots per-COUNTED-unit = per-each × `sub_unit_size`, fits `numeric(10,2)` unwidened, column comment documents basis) → **PASS** — jest test asserts `costPerUnitCounted` is stored verbatim (caller computes the bridge); migration confirmed NOT to widen the column (grepped, no `alter column … type` on `po_items.cost_per_unit`) and carries the exact documented `comment on column` (migration line ~907-911).
- AC10 (status transitions enforceable + observable: draft→sent→received|partial, cancel path releasing pending, close-short off partial; token set reconciled against live `'submitted'`) → **PASS** — `po_loop.test.sql::(S)` (CHECK rejects `'submitted'`, accepts `'cancelled'`); `(H)` (cancel refuses from `received`, P0001); `(H/P)` (a cancelled PO's quantity leaves `pending_po_qty`, suggestion rises back to baseline). Migration header documents the prod probe result (6 rows, all `'submitted'`) and the clean `'submitted'→'sent'` normalization — this is a factual claim from the prod-apply step that I cannot independently verify locally (local seed carries zero `purchase_orders` rows, confirmed by the migration's own header comment and by `grep -c purchase_orders supabase/seed.sql`), but the migration and pgTAP together prove the CHECK+normalization logic is correct given the developer's stated prod read.

Frontend (admin Cmd UI):

- AC11 (`POsSection` shows lifecycle state, offers send / mark-sent-manually / cancel-release / close-short per the resolved OQs) → **PASS** — jest `POsSection.test.tsx::action gating by status` (6 tests) exercises all five status values (`draft`+email, `draft`-no-email, `sent`, `partial`, `received`, `cancelled`) and asserts exactly the right button set renders for each. Cross-checked against the real component (`POsSection.tsx:187-190`): `canSend = status==='draft'`, `canCancel = ['draft','sent','partial'].includes(status)`, `canCloseShort = status==='partial'` — matches AC precisely, not a mock-of-a-mock.
- AC12 ("send to vendor" ALWAYS confirm-gated, emails via Resend + escaped HTML to `vendors.email`, flips to `sent`, NEVER auto-sent; empty-email → only mark-as-sent-manually) → **PASS** for the confirm-gating + email-presence-gating halves — `POsSection.test.tsx::actions are confirm-gated` (5 tests, incl. "does NOT call the store action when the user cancels the confirm") and the `DRAFT + NO vendor email` gating test (send hidden, no-email hint shown, mark-sent still offered). The escaped-HTML-email-send half is **NOT unit/integration tested** (no jest test invokes the real edge function; Deno edge functions aren't reachable from jest). I read `supabase/functions/send-po-email/index.ts` directly: every interpolated value (vendor name, item name, unit, qty via `escapeHtml(String(qty))`, `money()`-formatted costs) is routed through the inline five-char `escapeHtml`, `ADMIN_ROLES`/`requireAdminCaller` mirrors the CLAUDE.md convention exactly, the status flip is server-side and gated to `status==='draft'` only (a defensive addition beyond the literal AC text, correct), and `verify_jwt=true` is pinned explicitly in `config.toml`. This code-level verification is solid but is not exercised by an automated test — the spec itself marks "shell smoke optional for the receive round-trip" and does not name a required track for the email-send path, so this is a flagged gap, not a BLOCK against the spec's own stated scope.
- AC13 (`ReceivingSection` PO-driven mode: real `po_items`, ordered-qty prefilled, operator enters received qty, commit calls `receive_purchase_order`; freeform path retained as fallback) → **PASS** — jest `ReceivingSection.test.tsx` (5 tests): open-PO filter (`sent`/`partial` only, `draft`/`received`/`cancelled` excluded), outstanding-remainder prefill (`ordered − received`, asserted at the exact numeric value, not just "some value"), commit submits only non-zero this-receive deltas (verified against the exact call args `[{poItemId:'l1',receivedQty:10}]`), no-op at all-zero (commit not called). Cross-checked against `ReceivingSection.tsx:142` (`Math.max(0, ln.orderedQty - ln.receivedQty)`) and the freeform fallback (`FreeformReceivingMode`) is confirmed retained, not deleted, via `git diff` and the mode-toggle test.
- AC14 (received/closed-short/cancelled PO changes reorder within one realtime debounce, no manual refresh needed) → **PASS with a caveat.** `purchase_orders` is already in the `supabase_realtime` publication (confirmed via the migration's own header note and cross-referenced against `20260514140000_realtime_publication_tighten.sql`), and every lifecycle RPC mutates `purchase_orders`, so the existing `useRealtimeSync` debounce mechanically covers this with zero new subscription code — this is architecturally sound and I have no reason to doubt it given the existing realtime infra is unchanged. However, there is **no automated test (jest or pgTAP) that exercises the realtime channel itself** for this spec — realtime is not testable via jest (no WS harness in this repo) and pgTAP has no realtime layer. This was true for prior specs riding the same mechanism (087/102/104) and is a standing gap in the test infra, not new to 107. The store also does a deterministic `refreshPurchaseOrders()` + `loadReorderSuggestions()` call after every lifecycle mutation (confirmed in `useStore.ts:2436-2438,2462-2465,2476-2477,2488-2489`), which covers the same user-visible outcome without depending on realtime firing at all — so the AC is satisfied via a belt-and-suspenders path even if realtime were somehow disabled. Marking PASS on the strength of (a) the deterministic refresh path being directly readable and logically sound, and (b) the mechanism reuse being identical to already-shipped, already-verified realtime wiring — but flagging that no test directly proves the realtime leg.

### Additional coverage checks (from the prompt's specific callouts)

- **RLS pins on `purchase_orders`/`po_items`** → PARTIAL scope, matches spec's own design. `po_loop.test.sql::(R1)-(R4)` pin SELECT-only: member sees own-store PO (R1), non-member sees 0 rows for another store's PO (R2), master/privileged sees cross-store (R3), member reads child `po_items` scoped through the parent (R4). This does **not** include INSERT/UPDATE denial arms for a non-member — but the migration's own header (line 34) and the design's §0 explicitly scope this as "a REGRESSION PIN on existing policies, not new policy authorship," and the design opted for a SELECT-focused pin rather than a full CRUD sweep. This matches what was actually delivered; I am not treating the narrower scope as a defect since it matches the spec's own stated intent, but it is narrower than a "member/non-member/master, SELECT/INSERT/UPDATE" full sweep — worth the release-coordinator knowing the exact shape.
- **The reorder-body byte-parity assertion** → PASS, confirmed two ways: (1) I manually read and visually diffed both `pending_po_qty` CTEs in the migration (lines ~659-684 for `report_reorder_list`, ~1251-1276 for `report_reorder_for_counted_onhand`) — identical comment block and SQL body, byte-for-byte. (2) `po_loop.test.sql::(P)` proves parity on the OBSERVABLE RESULT (`48 == 140 − 92`) since the counted engine's flat item payload doesn't surface a `pending_po_qty` key directly (by design, per spec 105 Delta 2) — the test's own comment explains this shape and it is a correct workaround, not a weaker assertion.
- **FE-visible suggestion-reduction — GENUINE GAP, flagging per the prompt's specific ask.** The db.ts mapper (`db.ts:3526`, `pendingPoQty: Number(it?.pending_po_qty ?? 0)`) and `ReorderSection.tsx`'s `BreakdownLine` (`item.pendingPoQty` rendered in the "inbound" segment, line 81; also exported in CSV/PDF, line 553) are wired to display the real value — I confirmed this by reading both files directly. **However, no jest test asserts a non-zero `pendingPoQty` renders in the UI or that the displayed `order:` figure reflects a server-reduced `suggestedQty`.** The only test file that sets `pendingPoQty` at all (`ReorderSectionCases.test.tsx`) hardcodes it to `0` in both its fixture builders (`caseItem`/`plainItem`), and that file predates spec 107 (it's a spec-088 case-math test, unrelated to this spec's change) — it never varies the field. `ReorderSection.test.tsx` doesn't reference `pendingPoQty`/`inbound` at all. This means the pgTAP arm (P) proves the *RPC* returns the right numbers, but nothing proves the *FE actually displays* them rather than, e.g., silently dropping the field on a future refactor of the mapper or the breakdown line. Given the wiring is a straight one-line pass-through in both places, the risk is low — but per the project's own test-design rule ("every acceptance criterion maps to at least one test... a criterion with no test is a BLOCK") this specific FE-rendering leg of AC6/AC7's user-visible effect is **NOT TESTED** at the jest layer. I am not blocking the whole spec on this (the underlying data-correctness AC is proven by pgTAP, and the FE code is simple enough to verify by direct read), but I am flagging it explicitly as required by the release-coordinator's PASS/FAIL/NOT TESTED convention.
- **i18n parity (×3)** → PASS. Programmatically flattened and diffed `en.json`/`es.json`/`zh-CN.json` for the three new key namespaces (`section.reorder.createPo*`, `section.purchaseOrders.*`, `section.receiving.*`): 163 keys in `en`, zero missing and zero extra in `es` or `zh-CN`. `src/i18n/i18n.test.ts` (which enforces this generally) is green in the full jest run.
- **"Already ordered today" / legacy `createPurchaseOrder` → `'sent'` path** → PASS, verified by direct read, not just test-pass. `db.createPurchaseOrder`'s status literal was changed `'submitted'→'sent'` (`db.ts:1382`). I traced its only caller, `useStore.submitOrder` (`useStore.ts:2278`), and confirmed by grep it has no live call site anywhere else in the codebase — matching the design's own claim ("grep shows no live call site outside `useStore.ts`"). I also traced the actual "already ordered today" consumer, `record_missed_orders` (`supabase/migrations/20260530000000_record_missed_orders_rpc.sql:176-179`), and confirmed it predicates on `(store_id, reference_date, vendor)` row-existence only — **no `status` filter at all** — so the token rename is provably inert to that consumer. No test needed here since no runtime behavior changed on a live path; this was a correctness check, not a test-coverage gap.

### Test run

**pgTAP** (`npm run test:db`):
```
61/61 DB test file(s) passed
```
Includes `supabase/tests/po_loop.test.sql` — 30/30 assertions passed (matches the backend-developer's claimed count exactly). All 60 pre-existing suites also green, confirming no regression from the two reorder-RPC re-CREATEs or the RLS/status changes.

**jest** (`npx jest --silent`):
```
Test Suites: 80 passed, 80 total
Tests:       864 passed, 864 total
Time:        3.115 s
```
Matches the spec's claimed "80 suites / 864 tests." New files confirmed running and green: `src/lib/db.poLoop.test.ts` (11 tests), `src/screens/cmd/sections/__tests__/POsSection.test.tsx` (12 tests), `src/screens/cmd/sections/__tests__/ReceivingSection.test.tsx` (5 tests) — 28 new tests total, matching the claim.

**Typechecks:**
```
npx tsc --noEmit                          → exit 0, no output
npx tsc -p tsconfig.test.json --noEmit    → exit 0, no output
```
Both clean.

**No failures anywhere.** No test was modified or worked around to make it pass — all runs are as-shipped.

### Notes

1. **Test-design quality is high.** The jest section tests (`POsSection.test.tsx`, `ReceivingSection.test.tsx`) render real components and drive real button presses / text input, asserting on real rendered output and real store-call arguments — not a mock validating itself. I cross-checked every `testID` referenced by the tests against the real `.tsx` files and every db.ts function signature/payload shape the jest mocks assert against the real implementation; all matched. This is "test behavior, not implementation" done correctly.

2. **Two real design-vs-code discrepancies were caught and one materially fixed during the build** (self-reported by the backend-developer in the spec's "Two design contradictions resolved during build" section, and independently verified by me):
   - The `receive_client_uuid` coalesce direction was flipped from the design's literal snippet to match the design's own prose intent — a real idempotency bug that would have broken multi-receive dedup, now fixed and pinned by pgTAP.
   - The non-member refusal errcode is `P0002` (not `42501` as the spec's §10(e) prose states) due to INVOKER+RLS interaction. This is a behavior the shipped code correctly implements and pgTAP correctly pins, but it is a genuine AC-wording-vs-implementation divergence that the release-coordinator should surface to the architect/PM for a documentation reconciliation (not a code fix).

3. **Two gaps NOT TESTED at the jest layer, both low-severity given direct-read verification of the underlying code:**
   - The FE-visible rendering of a non-zero `pendingPoQty` in the reorder "inbound" segment (see "Additional coverage checks" above) — data correctness is pgTAP-proven, display correctness is unproven by any automated test.
   - The `send-po-email` edge function's actual HTTP behavior (escaping, role gate, Resend call, status flip) — no automated test exercises it at all; verified only by direct code read. The spec explicitly marks shell smoke for this surface as optional and does not name it a required track, so this is a scope-acknowledged gap rather than a spec violation.

4. **No test framework drift.** All new tests land in the three existing tracks (jest / pgTAP / — no new shell smoke was added, which is within the spec's stated optionality). No vitest/playwright/etc. introduced.

5. **Hard-rule files untouched.** `app.json` `slug` was not touched (confirmed via `git diff --stat`, not in the changed-files list) — matches the spec's explicit "app.json slug: untouched" note.

6. **Local-only verification.** This review ran entirely against the local Supabase stack (`npm run dev:db`, already running for this session). I did not verify the prod-apply steps (migration push, `schema_migrations` insert, edge function deploy, `RESEND_API_KEY` secret) — the spec correctly marks these as "USER-GATED — not run by this slice," and my review is scoped to the local dev-time correctness of the shipped code + tests, per this agent's charter.

### Verdict

No AC is FAIL. Two ACs (AC12's email-send path, AC14's realtime leg) and one cross-cutting concern (FE-visible `pendingPoQty` rendering) carry a **NOT TESTED** sub-component that I am surfacing explicitly rather than silently passing over, per the reporting convention. All are narrow, code-verified-by-direct-read, and none contradict the pgTAP/jest evidence — I am not blocking the spec on these, but the release-coordinator should weigh them as open risk rather than closed coverage when deciding SHIP_READY vs FIXES_NEEDED. Every other AC (11 of 14) has direct, behavior-driven automated test coverage that passed on this run.
