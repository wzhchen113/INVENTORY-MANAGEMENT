# Release proposal — spec 105 (par-status + inline reorder math on the count-history detail)

## Verdict
verdict: SHIP_READY
rationale: No reviewer flagged a Critical, all three code-reviewer Should-fixes and the architect's one Minor are resolved, and every suite is green post-fix (tsc clean, jest 798/798, pgTAP 59/59) — the only red signal is the user-gated prod-apply of a read-only RPC, a sequenced deploy step, not a code defect.

## Findings summary

- **code-reviewer:** 0 Critical, 3 Should-fix — **ALL RESOLVED** (see its `## Resolution` section) + 5 advisory nits (left as-is).
  - CR1 (null-total test used an unresolvable itemId, so the exclusion was gated by `!item` rather than `actualRemaining == null`) — FIXED: `item-apple-null` (parLevel 10, resolvable) added to the fixture so the null-total path is the operative gate; parStatus suite 15/15.
  - CR2 (`countHistoryPar.ts` misleading comment claiming `ReorderItem` types "leak at runtime") — FIXED: comment now states the real reason `formatQty` is kept local (avoids pulling `papaparse` into the pure module's / jest graph) and notes the type is compile-time-erased.
  - CR3 (pgTAP `(5)`/`(6)` label order vs execution order) — FIXED: header note clarifies the numeric labels are logical, not run-order (empty-map probe must run before the JWT switch).
  - Nits (all non-blocking, no code change requested): `case_qty` coalesce comment imprecision (`null` handled, `0` path unreached but correct); `?? 0` vs `?? null` at the `parStateFor` call site; `ParState` `'none'` vs spec's "no indicator" label; module-load smoke test asserts `typeof … 'function'`; caption terseness ("mixes … delivery timing" without "right now").

- **security-auditor:** 0 Critical, 0 Should-fix, 2 nits/confirmations. Ships clean.
  - `auth_can_see_store(p_store_id)` 42501 gate is the first executable statement, byte-identical to the engine, and pgTAP-verified with a **genuine non-member** plain-user JWT (manager, member of Towson+Frederick only, calling for Charles) — not a hollow assertion.
  - Grants byte-identical to `report_reorder_list` (`revoke … from public, anon; grant … to authenticated`) — **anon cannot execute**; not broadened.
  - `p_on_hand` is a bound `jsonb` param consumed via `?` / `->>` + `::numeric` — **no SQLi surface** (no `EXECUTE`, no `format()` on map contents); a hostile value at worst raises a clean `22P02`.
  - Item-map is a **filter on the store's own items, not a driver** — foreign/garbage ids yield an empty `items[]`, no cross-store leak; the on-hand echo is the caller's own supplied value.
  - `security invoker` + `set search_path = public`; **no cost/`$` fields** in output (Delta-1 drops `cost_per_unit`); no PII (item names deliberately not in payload); FE degrades read-only (`console.warn` + `setReorderByItem({})`, no toast) so par badges still render.

- **test-engineer:** **14/14 acceptance criteria PASS.** pgTAP 59/59 (9 assertions in the new suite + all 7 sibling reorder suites, no regression), jest 798/798 (75 suites; the added parStatus suite covers 3 par states + null-total + unresolvable + map-build + suggestion-string (no `$`) + companion-fetch flow incl. requested-but-absent collapse and failure/empty degradation), `tsc --noEmit` clean. Documented gaps, all judged non-blocking: browser verification structurally blocked (0 seed `inventory_count` records) → validated via the live local RPC + jest; no jest pin on the exact AC10 caption string (behavioral criterion satisfied by code); `reports_anon_revoke.test.sql` suite not yet extended to catalogue this function's signature (revocation verified ad-hoc + covered by the 42501 gate); pgTAP usage-forecast depth exercised only through the `usage_per_portion=0` path (broader coverage lives in the sibling engine suites on the shared CTE chain).

- **backend-architect (drift):** 0 Critical, 1 Should-fix + 4 Minor. **The verbatim-copy-with-two-deltas discipline holds** — every forecast/case/next-delivery CTE (`par_replacement` / `usage_forecasted` / `suggested_qty` / `suggested_cases` / `suggested_units`) is **byte-identical** to `report_reorder_list`, with the two intended deltas (on-hand from `p_on_hand`; flat item-keyed output collapsing multi-vendor items to `min(days_until)`) isolated exactly where the design placed them; no `$` leaks. Resolution: **M1 (stale pgTAP header `case_qty=40` vs actual `24`) FIXED**; **S1 (multi-vendor forecast-collapse pgTAP coverage gap) DEFERRED as a follow-up** — the "soonest truck" collapse is correct/intentional and `days_until` itself IS tested; S1 is a regression-protection coverage add (pin that the collapse surfaces the soonest vendor's forecast quantity) needing a POS-fixture + `usage_per_portion > 0` multi-vendor case, not a bug and not blocking v1. M2 (disclosed filename shift `…0701` → `…0702`), M3 (prod-apply pending → gate red, expected/user-gated), M4 (`todayIso()` device-local, matches the existing engine-path behavior) — no action.

## Recommended next steps (ordered)

SHIP_READY. The prod-apply of the RPC is a **user-gated deploy step**, not a code blocker — but it MUST land, and both CI gates MUST confirm green, before this ship is considered complete. Execute in order:

1. **User authorizes the commit.** Per the staging convention, the edits are staged and ready; do not `git commit` without the user's explicit go-ahead. This commit **bundles the parked count-history `each` → `loose units` label fix** (already staged) together with the spec-105 work, per the user's earlier decision.
2. **Push to `main`.** (Migration `20260702000000_report_reorder_for_counted_onhand.sql` is a read-only `create function` — additive, no data migration, no backfill, rollback = `drop function`.)
3. **Apply the RPC to prod (`ebwnovzzkwhsdxkpyjka`) via the Supabase MCP.** Run the migration SQL via `execute_sql`, then insert the exact version row (`20260702000000`) into `supabase_migrations.schema_migrations`, and verify the function exists with the expected signature (`report_reorder_for_counted_onhand(uuid, jsonb, jsonb)`, `security invoker`, granted to `authenticated` only) — per the prod-migration-via-MCP memory (`db push` lacks the prod password). Until this lands, the `db-migrations-applied` gate sits RED **by construction** (the migration is committed but intentionally not-yet-applied) — this is the disclosed, sequenced deploy step, NOT the silent committed-but-never-pushed drift that gate was written to catch.
4. **Confirm BOTH active gates are green on `main`** — `.github/workflows/test.yml` **and** `.github/workflows/db-migrations-applied.yml` — via `gh run list --branch main --workflow <file> --limit 1` for each. The `db-migrations-applied` gate flips from RED to green only after step 3 (prod-apply + version-row insert). Do not consider the ship complete until both are green; if either is red or in-progress after step 3, surface the run URL and wait for user direction.

### Follow-ups (NOT blocking this ship)
- **S1 — multi-vendor forecast-collapse pgTAP coverage add.** Add one assertion: a multi-vendor item with `usage_per_portion > 0` and a non-zero POS rate, asserting the collapsed `suggested_qty` equals the **soonest** vendor's forecast (not the max across vendors). Pins the collapse semantic against a future refactor that might silently switch to `max` / `order by days_until desc`. The behavior is already correct and `days_until` is tested — this is regression protection only.
- **Extend `reports_anon_revoke.test.sql`** to catalogue `report_reorder_for_counted_onhand`'s signature (its anon revocation is currently verified ad-hoc + covered by the 42501 gate, but not in the formal per-report catalogue suite). Fold into the next maintenance pass on that suite.
- **Seed fixture for `inventory_count` records** (optional). The prod-pulled seed has 0 saved counts, so the history-detail `DetailFrame` is unreachable in the browser and this feature could not be UI-smoke-verified (validated via the live RPC + jest instead). A minimal seeded count would unblock browser verification for this and future count-history work; worth adding if UI smoke coverage of the detail view is wanted, but not required for correctness here (pure-function + pgTAP + live-RPC coverage stands in).

## Out of scope for this review
- The optional caption-caveat about the "soonest vendor's quantity" collapse (architect S1, item 1) — deferred with the S1 coverage add; the current dual-basis caption already satisfies AC10 (current-par basis AND live-forecast/timing basis). A wording refinement, not a fix.
- The device-vs-store timezone approximation in `todayIso()` (architect M4) — carries over from the existing `report_reorder_list` caller path; any tz-correctness change belongs to a cross-cutting spec touching all reorder callers, not this one.
- Par status on the live count form (`count.tsx`) and the EOD / staff weekly count — explicit follow-ups per OQ-3, different UX and data path.
- Any `$` / cost display, sort/filter/badge-count by par status, and export of the indicator or suggestion — explicitly out of scope per the spec (spec 104's per-each cost basis stays disengaged).
