## Verdict
verdict: SHIP_READY
rationale: The sole reviewer Critical is resolved and browser-verified, all four Should-fix items are closed, and every suite is green — remaining work is the user-gated prod apply, a required pre-ship operational step rather than a code blocker.

## Findings summary
- code-reviewer: was 1 Critical + 4 Should-fix + 6 Nits; **Critical and all 4 Should-fix now RESOLVED** (see the "## Resolution (applied by main Claude post-review)" section appended to code-reviewer.md). The Critical was `WasteLogSection.tsx:82` — the live waste preview `previewCost` was missing the `× subUnitSize` bridge, so a `subUnitSize > 1` item read `sub_unit_size×` too low before submit; fixed with `× (pickedItem.subUnitSize || 1)` and browser-verified (2oz Cup, sub 2000, qty 1 → preview `-$33.00`, was `-$0.02`). Should-fix (all fixed): `VendorsSection.tsx:386` denominator label now `subUnitUnit || 'each'`; the two `costPerUnitCol` i18n keys (inventory + vendors, all three langs) reworded to per-each; `WasteLogSection.tsx:70` `totalWeekCost` gained the `Spec 104 (R1)` frozen-snapshot comment. The stray-slash typo was moot (not present in the current file). Post-fix gate: tsc clean, jest 783/783. Only advisory Nits remain (redundant `calcUnitCost` guard, `logWasteEntry` extra read, audit-table PK note, `avgCost` fallback comment).
- security-auditor: **0 Critical, 0 Should-fix, 2 Nits — PASS, nothing blocks.** Audited the prod-touching migration (column widening, option-(b) re-derivation, `report_reorder_list` + `staff_log_waste` re-CREATEs, audit table), the `db.ts` write-side bridges, and the pgTAP grant allowlist. Both RPC re-CREATEs preserve byte-identical signatures (so `create or replace` preserves ACLs — no SECURITY DEFINER function silently re-exposed), pin `search_path`, and keep the auth model identical (`auth_can_see_store` gate on reorder; `staff_log_waste` service_role-only + dormant since spec 061). Audit table is grant-locked (deny-all to anon/authenticated, no PII) and correctly allowlisted with a matching negative assertion (arm 7). Migration is reversible, idempotent (UPDATEs write the frozen `new_cost`, so re-apply is a no-op), and fully inside one txn. No dynamic SQL, no cross-store exposure, no secret/grant leak in the seed regen. Nits: audit-table confidentiality is CI-enforced not schema-enforced (arm-7 assertion must never be deleted); POs/Receiving per-unit cosmetic — dismissed, no dollar drift.
- test-engineer: **22/25 ACs PASS; jest 783/783 (74 suites), pgTAP 58/58, both typechecks clean.** 3 NOT-TESTED are non-blocking per the test-track scope: AC6 (`getPrepRecipeCost`/`getPrepRecipeCostPerUnit` roll-up render — covered indirectly via the tested `getIngredientLineCost` call graph, but no dedicated roll-up regression); AC8 (waste write-side R1 fix — correct by inspection + local DB verification `49.00` snapshot, no automated jest/pgTAP test); AC21 (prod `schema_migrations` gate — prod apply pending). Coverage gaps of the same shape noted for completeness (population-X zero-cost fixture, `numeric(12,6)` `information_schema` assertion) but each is a simple predicate/DDL correct by inspection and none is mandated by the spec's test-track ACs. The two `.test.tsx`/`.test.ts` narrowings (spec093 `×`-glyph ban, `perEachCost` fallback) are confirmed legitimate scoped updates, not regression-masking.
- backend-architect (drift): **0 Critical, 0 Should-fix, 2 Minor (both documented at call sites) — implementation matches the REVISED design with high fidelity.** Widen-first `numeric(12,6)` before any re-derivation; option (b) `cost_old / sub_unit_size` on all three targets (not from `case_price`); audit-keyed idempotency on all three UPDATEs; both reorder additive hunks unconditional with the spec-102 multi-vendor body intact (no silent 088/100/102 revert); BACKOUT restores values then re-narrows. `numeric(12,6)` sufficiency proven (per-store reorder totals round-trip to the cent, max $0.000050/row reconstruction error — the prior `(10,2)` +$68.54 Frederick drift is gone). Minor-1: §7's flat consumer list *named* `weightedCost` as a bridge target, but the code's exclusion (cost-average numerator tracks the basis, only stock-VALUE totals get the bridge) is architecturally correct — spec-text imprecision, not code drift. Minor-2: POs/Receiving per-unit display cell vs bridged line total — cosmetic, no dollar-total drift, correctly deferred. The `staff_log_waste` `ci.name/ci.unit` change is a necessary in-envelope schema-drift fix (P3 dropped `inventory_items.name/unit`), not unmanaged drift.

## Recommended next steps (ordered)
SHIP_READY — no code fixes required. Execute the pre-ship steps in order; do NOT skip the prod apply or the final gate check.

  1. **User authorizes the commit.** Main Claude does not auto-commit on SHIP_READY (project hard rule). All spec-104 edits are staged and green (tsc clean, jest 783/783, pgTAP 58/58); await the explicit "commit it".
  2. **Push to `main`.** Standard push after the authorized commit.
  3. **Apply the migration to prod (`ebwnovzzkwhsdxkpyjka`) via the Supabase MCP** — `20260701000000_spec104_per_each_cost_basis.sql` (execute_sql for the migration body, then insert the exact `20260701000000` version into `supabase_migrations.schema_migrations`, per the project's prod-migration-via-MCP procedure — `db push` lacks the prod password). This migration re-derives cost data and **widens `inventory_items.cost_per_unit` + `item_vendors.cost_per_unit` to `numeric(12,6)`**. **The column widening is a table rewrite under ACCESS EXCLUSIVE (architect R9) — run off-peak.** The migration is one-shot against old-basis rows and idempotency-guarded (a re-run snapshots 0 new audit rows and the UPDATEs write the frozen `new_cost`, so no double-divide). Note the documented local caveat: do NOT hand-re-apply on a seeded local DB (post-`db reset` the audit is empty, so seed rows would divide twice) — the supported local path is `db reset` alone.
  4. **Confirm BOTH CI gates are green on `main` after the prod apply** — `test.yml` AND `db-migrations-applied.yml` — via `gh run list --branch main --workflow <file> --limit 1` for each. `db-migrations-applied.yml` will sit RED between step 2 (push) and step 3 (prod apply) because the repo migration is not yet in prod's `schema_migrations`; it re-greens only after the MCP `schema_migrations` insert in step 3. Both gates must be green before the ship is considered complete. (Per CLAUDE.md, a green `test.yml` alone is NOT sufficient evidence — the db-migrations gate is an independent signal.)

Follow-ups (NOT blocking ship):
  - AC6/AC8 automated regression: add a jest roll-up test for `getPrepRecipeCost` with per-each seeds, and a pgTAP `staff_log_waste` write-side snapshot test. Both paths are correct by inspection + local verification today.
  - Address the advisory Nits at a convenient pass (redundant `calcUnitCost` guard comment, `avgCost`/`weightedCost` linking comment, audit-table PK-order note).

## Out of scope for this review
- **spec-102 empty-`item_vendors`-on-reset gap** — surfaced independently by code-reviewer (pgTAP `vendor_total_cost` scoping observation) and confirmed by backend-architect: `item_vendors` has 0 rows in `seed.sql` and is populated only by the spec-102 backfill, which sees zero rows on a fresh reset (migrations run before seed). Spec 104 does not touch that backfill path (it only re-derives the `cost_per_unit` value on existing rows) and its reorder pgTAP self-seeds its own links precisely to avoid depending on the junction. Genuinely orthogonal — belongs in a spec-102 follow-up (regen seed to include `item_vendors`, or add the backfill to seed), NOT here.
- **POs/Receiving per-unit display cell** (security-auditor Nit #2 / architect Minor-2 / test-engineer note 5) — the per-unit cost cell shows the raw per-each figure next to a bridged line total, so `unitCost × qty ≠ lineCost` *visually* for `sub_unit_size > 1` items. Confirmed by architect to be display-only with NO dollar-total drift (totals are bridged correctly). Reconciling the per-unit display cell is outside spec 104's "every consumer-visible dollar TOTAL stays unchanged" contract — a documented cosmetic follow-up.

## Handoff
next_agent: NONE
prompt: SHIP_READY — 0 live Criticals (the one code-reviewer Critical + 4 Should-fix are resolved and browser-verified), all suites green (jest 783/783, pgTAP 58/58, tsc clean). Pre-ship steps ordered: user authorizes commit → push → prod-apply migration via Supabase MCP (column-widening rewrite under ACCESS EXCLUSIVE — off-peak) → confirm BOTH CI gates green (db-migrations-applied stays red until the prod apply + schema_migrations insert). spec-102 empty-item_vendors gap and POs/Receiving per-unit display are orthogonal follow-ups.
payload_paths:
  - specs/104-per-each-cost-basis-rework/reviews/release-proposal.md
