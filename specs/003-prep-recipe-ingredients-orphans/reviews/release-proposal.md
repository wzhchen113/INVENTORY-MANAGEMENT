## Verdict
verdict: SHIP_READY
rationale: All four reviewers returned 0 Critical; migration is already applied to prod with verify_orphan_count = 0 and no canonical-row collateral, so the question is "commit the shipped artifacts" not "should we ship".

## Findings summary
- code-reviewer: 0 Critical, 3 Should-fix, 5 Nits. Top issues: (1) the apply-context canonical sanity check uses `SELECT COUNT(*) ... IF v_canon_visible_count < 7` instead of the `EXISTS ... HAVING COUNT(*) >= 7` form recorded as a Spec 001 lessons-learned rule; (2) the unused-outside-its-branch `v_canon_visible_count` declaration that the EXISTS rewrite would eliminate; (3) the success NOTICE hardcodes "across 7 preps" as a literal and uses `matching-deduped / divergent-discarded` vocabulary instead of the spec §4 template's `repointed / deleted` neutral wording. All three Should-fix items target the already-applied SQL file.
- security-auditor: 0 Critical, 0 High, 0 Medium, 1 Low (advisory only — note that no `audit_log` row is written; matches Spec 001/005/006 convention and the `audit_log` policy doesn't fit a brand-shared superuser-context migration cleanly). RLS posture verified clean (architect's §10 claim confirmed by direct read of the per-store hardening migration). SQLi structurally impossible. Brand-scope leakage path closed both by classification predicate and §9 verification. Per-prep assertion correctly placed before mutation. No new secrets, no client/edge code touched, no `package.json` change.
- test-engineer: 13/15 ACs PASS (VERIFIED / CODE-VERIFIED / N/A). 2 NOT-TESTED (AC14 → security-auditor, AC15 → architect; both reviewers completed clean and effectively close those ACs). 0 FAIL. One Should-fix on AC3 — the migration's `v_canon_visible_count < 7` aggregate guard is weaker than the spec's "exactly one current row per name" wording (a 0+2 split would slip past), but probe-time gate_2 already certified per-name = 1 for all 7 names and the migration is now permanently in the no-op path on re-run, so the residual risk is near-zero. Orphan math confirmed: 464 pre-apply − 399 deleted = 65 remaining (47 canonical + 18 non-affected), zero collateral on canonical rows.
- backend-architect: 0 findings at any severity. Implementation matches design verbatim. The four "deviations" cited at handoff are all non-substantive: (1) timestamp bump to sort after Specs 005/006 was forced by their landing between design and build; (2) DELETE-only body was an explicit §5 design finding, not a deviation; (3) pre-mutation per-prep assertion ordering is a strict improvement over the §7 sketch and "should be the project pattern going forward"; (4) no recovery snapshot aligns with the §12 Q7 resolution. Forward-looking: the 52 non-current `prep_recipes` rows are now zero-referenced by both child tables, making a future cleanup spec strictly safer than at design time.

## Recommended next steps (ordered)

1. **Commit and deploy as-is.** No reviewer flagged Critical, High, or Medium. The SQL is already on prod with all §9 verification probes green; modifying the shipped migration file post-apply is the larger risk surface (creates a divergence between what's recorded in git and what actually ran on prod). Recommended commit content: `specs/003-prep-recipe-ingredients-orphans.md`, `specs/003-prep-recipe-ingredients-orphans/reviews/*.md` (all four reviewer files plus this proposal), and `supabase/migrations/20260507040000_spec003_repoint_or_delete_ingredient_orphans.sql` exactly as applied.

2. **(Optional, doc-only inline cleanup before commit.)** Code-reviewer Nit #1 noted a one-word terminology split inside the migration's header comment ("repoint" vs body's "DELETE matching orphans") that is fixable by a parenthetical clarification — this is comment-only text inside the SQL file. Strictly speaking, even that touches the already-applied migration file; if perfect git-vs-prod parity is the priority, skip it. If header-comment-only edits are considered safe (the SQL body is unchanged), it can be folded inline. Recommend skipping by default to preserve the "what's in git is byte-identical to what ran" guarantee — same posture as Spec 006.

3. **(Follow-up, NOT blocking ship.)** Capture the architect-endorsed convention in project memory: per-prep affected-count assertions BELONG before mutation in data-repair migrations going forward (Spec 003 set the norm; Specs 001/005/006 used different orderings). Worth a one-line entry in CLAUDE.md or the project's lessons-learned section the next time it's edited — not in this commit, since CLAUDE.md edits are out of Spec 003's scope.

4. **(Follow-up, NOT blocking ship.)** When the AC3 aggregate-vs-per-name pattern next appears in a similar migration template, switch to per-name `EXISTS ... HAVING COUNT(*) = 1` enforcement at runtime. Surface as a template-improvement note for future data-repair specs; do not amend Spec 003's shipped SQL.

5. **(Follow-up, NOT blocking ship.)** Architect's forward-looking note: a "delete unreferenced non-current `prep_recipes`" spec is now strictly safer than it was at Spec 003 design time, because both child tables (`recipe_prep_items` post-Spec-001, `prep_recipe_ingredients` post-Spec-003) are zero-referenced against those 52 rows. User has already explicitly out-of-scoped this for Spec 003 (spec line 133); flag for the user's awareness as a candidate for a future spec.

## Out of scope for this review
- The 3 code-reviewer Should-fix items targeting the already-applied migration SQL (EXISTS-vs-COUNT guard form, unused variable, NOTICE wording). Modifying SQL on a migration that has already executed against prod creates a git-vs-prod divergence; the correct venue for the EXISTS pattern is a future migration template, not a retroactive edit here.
- The AC3 aggregate-vs-per-name canonical check Should-fix (test-engineer). Same rationale: future-template improvement, not retroactive edit.
- Audit-trail / `audit_log` policy convention (security-auditor's 1 Low). Project-meta note for if/when a structured migration-audit table is introduced; no action required for Spec 003.
- The 52 zero-referenced non-current `prep_recipes` rows (architect's forward-looking note). User explicitly out-of-scoped these in spec line 133. Candidate for a future spec.
- Capturing the pre-mutation per-prep assertion ordering as a project convention in CLAUDE.md. Worth doing in a separate edit, not bundled into the Spec 003 commit.
- Re-running Spec 002's catalog-wide `prep_recipe_id` resolution jq guard against the live local stack post-Spec-003 (test-engineer flagged as available belt-and-suspenders confirmation; architect's mathematical argument that no canonical row was touched is sound).
- The Specs 005/006 implicit prerequisite coupling (test-engineer Note #4) — the spec's `## Dependencies` section was written pre-Spec-005/006 and does not formally list them. The filename timestamp ordering already enforces correct ordering at apply time, and a manifest mismatch under wrong ordering would loud-abort. Informational only.

## Handoff
next_agent: NONE
prompt: SHIP_READY — all four reviewers 0-Critical; migration already applied to prod with §9 probes green; recommend commit-as-is (or with optional doc-only header-comment cleanup if perfect git-vs-prod parity is not required). 3 code-reviewer Should-fix and 1 test-engineer Should-fix all target the already-applied SQL and are recommended as future-template improvements rather than retroactive edits.
payload_paths:
  - specs/003-prep-recipe-ingredients-orphans/reviews/release-proposal.md
