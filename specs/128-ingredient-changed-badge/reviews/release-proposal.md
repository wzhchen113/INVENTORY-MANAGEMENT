## Verdict
verdict: SHIP_READY
rationale: Zero Critical findings across all four reviewers, all 8 acceptance criteria PASS, the two actionable Should-fix items and the security Low were resolved post-review, and the test suite (pgTAP 20/20 + full 74 files, jest 120/1285, tsc/typecheck:test/i18n parity) is green.

## Findings summary
- code-reviewer: 0 Critical, 2 Should-fix, 2 Nits. Both Should-fix were documentation-accuracy only — SF#1 (migration comment overclaiming spec-122 trigger coverage) verified a MISREAD, no such claim exists (NO CHANGE NEEDED); SF#2 (pgTAP header implying an absent "both photo+vendor changed" fixture) RESOLVED by rewording the header to match actual coverage, pgTAP still 20/20. Nits (redundant asymmetric `.catch` on the Weekly call site; `WHEN`-clause vs in-body guard style) are non-blocking.
- security-auditor: 0 Critical, 0 High, 0 Medium, 1 informational Low. Low (two non-DEFINER trigger fns didn't pin `search_path`, no reachable vector) RESOLVED as defense-in-depth — added `set search_path = pg_catalog` to both trigger fns, re-applied locally, pgTAP still 20/20. Invoker RPC rides existing per-store RLS (no cross-store/cross-brand leak), triggers carry no user input or dynamic SQL, client degrades to empty Set on error, no new table/policy/grant/PII.
- test-engineer: 8/8 acceptance criteria PASS, 0 FAIL. Highest-risk ACs (badge clears on store count incl. max-over-both-count-kinds + draft exclusion; no false-stamp on cost-only/same-vendor writes; fetch never blocks the count screen) directly and solidly covered. Disclosed non-blocking gaps: no two-store side-by-side pgTAP fixture (cross-store isolation is a structural `store_id` WHERE-clause guarantee, same posture as existing RLS suites) and no live-browser visual check of the badge (staff surface unreachable via admin-only local login — same disclosed limitation as spec 127).
- backend-architect: 0 Critical, 0 Should-fix, 3 Minor. Implementation byte-consistent with the approved design on every load-bearing point; all six requested confirmations hold (migration columns/triggers, invoker RPC greatest()/last-counted semantics, stateless derived clearing/no ack table, `updated?` on staff types only, best-effort merge + deferred banner, no realtime/publication change). Minors (i18n key `chrome.count.updatedBadge` vs design's `chrome.updatedBadge`; INNER join drops NULL-`catalog_id` items — unreachable post-P3-lockdown; asymmetric best-effort `.catch`) are cosmetic/harmless.

## Recommended next steps (ordered)
1. Commit and deploy (user confirms the commit — main Claude does not auto-commit).
2. Prod-apply migration `20260722000000_ingredient_changed_badge.sql` via Supabase MCP `execute_sql`, then insert the exact version string `20260722000000` into `supabase_migrations.schema_migrations` to keep the `db-migrations-applied` gate green. HARD ORDERING: this MUST land AFTER spec 127's `20260721000000` (128 references `catalog_ingredients.image_path` added by 127; 127 is already in prod, so ordering is satisfied). No edge redeploy, no realtime restart. After the push, confirm the latest run of BOTH gates (`test.yml` and `db-migrations-applied.yml`) on `main` is green.
3. (optional, non-blocking follow-ups) Reconcile the design doc's `chrome.updatedBadge` key reference with the shipped `chrome.count.updatedBadge` (architect M1); pick one convention for the best-effort `.catch` so EOD and Weekly call sites match (code-reviewer nit / architect M3).

## Out of scope for this review
- A two-store side-by-side pgTAP fixture to empirically prove cross-store badge non-leakage — the `where ii.store_id = p_store_id` predicate makes it structurally guaranteed; a dedicated multi-store DB fixture would belong in a broader RLS-coverage spec.
- A staff-login preview path enabling live visual verification of badge render / no-layout-shift on EOD + Weekly rows — an environment/tooling gap shared with spec 127, not specific to this feature.
- NULL-`catalog_id` inventory item handling (architect M2) — unreachable in prod post-P3-lockdown; only relevant to a future legacy-data or partial-migration scenario.
