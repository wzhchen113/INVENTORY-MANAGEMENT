## Verdict
verdict: SHIP_READY
rationale: All four reviewers are green (0 Critical across the board), the one Should-fix is resolved with a verified artifact, and every gate is green locally — with the mandatory prod-apply of the security migration folded into the ship checklist as a hard gate, not a nicety.

## Findings summary
- **code-reviewer:** 0 Critical, 1 Should-fix (RESOLVED), 3 Nits (left, all out-of-scope by their own framing). The one Should-fix — dead `receiving.col.{item,ordered,received}` i18n keys defined in all three staff catalogs but never rendered — is fixed: those three keys AND a fourth the review didn't list (`col.outstanding`, independently verified unreferenced) were removed from all three catalogs; `col` now holds only the two live keys (`receiveNow`, `receiveNowAria`). Line-by-line diff of the migration vs its stated source (`cost_on_receipt.sql:122-386`) confirmed the ONLY delta is the 15-line gate hunk at the designed site; pinned string byte-exact in migration + `comment on function` + both pgTAP call sites.
- **security-auditor:** 0 Critical, 0 Should-fix, 2 informational nits. PASS. The load-bearing R-1 privilege-escalation fix was probed against the LIVE local stack as the real non-privileged (`user`) and privileged (`master`) identities across all 7 cases, including the crux (the §3a stock UPDATE runs earlier in the same loop, yet the `raise` aborts the implicit transaction and rolls it back — nothing durable). Presence-not-value confirmed (`0`/`-1`/`39`-equal/`999` all raise identical `42501`, `-1` returns `42501` not `P0001` — gate fires before the `<0` check). No same-price bypass. SECURITY INVOKER + `search_path=public` + ACL (`authenticated`/`postgres`/`service_role`, no `anon`/`public`) preserved. Frontend belt verified (exactly two keys per line, never `new_case_price`). **Flagged: prod is NOT applied — the escalation hole stays open in prod until the MCP apply of `20260707000000` lands.**
- **test-engineer:** 15/15 acceptance criteria PASS, 0 FAIL, 0 NOT TESTED. test:db 64/64 files (new `staff_receiving_gate.test.sql` 45/45, deterministic on standalone re-run; spec-109 `cost_on_receipt.test.sql` still 55/55 as the byte-identical admin-path regression). jest 92 suites / 1031 tests, ran twice identical. Both typechecks exit 0. AC-2's "nothing durable" is a genuine read of real post-refusal table state across all six durable targets, not an exception-type-only check. Two low-severity non-blocking gaps: no dedicated `StaffStack.test.tsx` for the 4th-tab wiring (3 lines of declarative JSX, hand-diffed + browser-verified), and native-EAS render not separately exercised (project-level precedent, not spec-113-specific).
- **backend-architect (post-impl drift):** 0 Critical, 0 Should-fix, 3 Minor (all informational, nothing to change). Verdict IN DESIGN — no drift. Independent region-by-region alignment confirms the body is byte-identical to `cost_on_receipt.sql:122-386` except the one gate hunk at the fixed site (first statement in the §3b branch, before the `<0` check); refusal errcode/string/predicate byte-exact; signature stable so `create or replace` preserves the ACL with no grant re-emit; no schema/RLS/publication change; spec-109 pgTAP suite confirmed unmodified (`plan(55)`).

## Recommended next steps (ordered)

SHIP_READY — but this spec has BOTH a backend surface (the RPC body re-CREATE) AND is a security fix, so the prod-apply is a MANDATORY ship-gate, not an optional follow-up. The fix is not live where it matters until step 2 lands.

1. **Commit + push the change set** (backend migration + pgTAP + staff carve-out/screen/nav/i18n/jest). This is the user's call to commit — main Claude does not auto-commit on SHIP_READY.
2. **After the push, confirm both CI gates on `main`:** `test.yml` green AND `db-migrations-applied.yml`. **Expected:** `db-migrations-applied.yml` will go RED after the push, because `20260707000000_staff_receiving_price_gate.sql` is committed to the repo but not yet in prod's `schema_migrations` — that is the exact drift the gate exists to catch, and it closes in step 3. Do not treat this red as a defect; it is the gate working. `test.yml` must be green independently.
3. **Prod-apply the security migration (user-gated — REQUIRED for the fix to be real).** Until this lands, a staff `user` in prod can still rewrite item costs via a crafted `new_case_price` (security-auditor, explicit). Because this is a body-only re-CREATE, it is INVISIBLE to the migration-list drift gate, so:
   a. Apply via Supabase MCP `execute_sql` against `ebwnovzzkwhsdxkpyjka` (`db push` lacks the prod password — project MEMORY).
   b. INSERT the exact version `20260707000000` into `supabase_migrations.schema_migrations`.
   c. POST-APPLY verify the body carries the gate — normalized-md5 local-vs-prod OR the header's probe: `select 1 from pg_proc where proname = 'receive_purchase_order' and pg_get_functiondef(oid) like '%forbidden: price change requires admin%';`
   d. Re-green `db-migrations-applied.yml` (the step-2 red closes once the version row is in prod).

## Out of scope for this review
- **Pre-existing ungated cost writer `create_inventory_item_with_catalog`** (security-auditor nit, migration `20260504173843…`, predates this spec) — SECURITY INVOKER, EXECUTE to `authenticated`, no privilege gate, so a store `user` could craft an item-*creation* with an arbitrary `case_price`/`cost_per_unit`. This is the item-creation path, entirely separate from the receive/re-price path spec 113 closes; not a regression here. Spin off as its own security spec — it does NOT block this ship.
- **Nits left by code-reviewer (all out-of-scope by their own framing):** no `.limit()` on the two unbounded carve-out reads (worth a follow-up only if a store ever accumulates many open POs — v1 counts are tiny), the duplicated `createdAt.slice(0,10)` date fallback, and `shortPoId` not being unit-tested independently.
- **No dedicated `StaffStack.test.tsx` for the 4th-tab wiring** and **native-EAS render not separately exercised** (test-engineer notes) — both low-severity, the latter a project-level precedent gap, neither introduced by this spec.
- **Optional Playwright e2e** of the open-PO → receive → refresh happy path — noted optional in the spec; jest + pgTAP are the required coverage and both pass.

## Handoff
next_agent: NONE
prompt: SHIP_READY — commit + push, confirm test.yml green (db-migrations-applied will go RED on push until the prod-apply lands — expected), then user-gated prod-apply of 20260707000000 via Supabase MCP + schema_migrations insert + pg_get_functiondef verify; the security fix is not live in prod until that step.
payload_paths:
  - specs/113-staff-receiving/reviews/release-proposal.md
