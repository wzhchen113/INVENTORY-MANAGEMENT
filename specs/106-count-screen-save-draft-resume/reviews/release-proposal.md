# Release proposal — spec 106 (count-screen save-draft + resume)

## Verdict
verdict: SHIP_READY
rationale: Every reviewer Critical (code-reviewer C1 wrong-connectivity-signal, test-engineer TS2556 build break + AC-9/AC-17 NOT-TESTED, architect SF-1) is resolved in the post-review fix pass and independently re-verified — jest 836/836, pgTAP 60/60, both typechecks exit 0, admin flow browser-verified — with the only remaining item a sequenced, user-gated prod-apply, not an open defect.

## Findings summary
- **code-reviewer**: 1 Critical, 3 Should-fix, 3 Nits (all pre-fix).
  - Critical (admin Save online/offline branch + reconnect-sync driven by `useConnectionStatus`, which reports the Supabase realtime-SOCKET state, not network connectivity → false-negative offline branch on web websocket blips, always-true on admin native) → **FIXED**: `onSaveDraft` rewritten server-first with local-fallback-on-error (attempts the server write unconditionally; on a network-type rejection writes the unsynced local copy + offline toast — the AC-14 observable, no connectivity oracle on the save path). The socket hook is now only the reconnect-sync trigger, gated by the SF-1 restore-once guard.
  - Should-fix #1 (AC-6 first-uncounted jump not implemented on admin) → **IMPLEMENTED**: `jumpToFirstUncounted` (reuses shared `firstUncounted`) + `pendingFocusId`/`firstInputRefs` + focus-on-restore effect.
  - Should-fix #2 (discard-failure silent resurrection: form cleared but server row survives → re-fetched on next open) → **FIXED**: `onDiscardDraft` now server-first — awaits `deleteCountDraft`, clears local+form only on success, keeps banner+values + `discardFailed` toast on failure. Both screens.
  - Should-fix #3 (`countDrafts.ts` deserialize doc/test-title claimed unknown-`v` tolerance the code doesn't implement) → **FIXED**: doc comments + test title corrected to describe forward-tolerant field reading (no `v`-gating). No logic change.
  - Nits (dead `twoItems()` helper; `reconcileDrafts` local-only "what-if" comment; bare `'SAVING…'` literal) → **FIXED** (dead helper removed, comment tightened, in-flight label routed through `section.countDraft.saving`).
- **security-auditor**: 0 findings at any severity. Verdict PASS. Owner-scoped RLS (`auth.uid() = user_id` whole-clause, no admin/super_admin bypass) proven by real pgTAP assertions (cross-user SELECT/UPDATE/DELETE = 0 rows, spoof-insert 42501, super_admin JWT SELECT = 0); no HTML sink / no dynamic SQL / no service-role key / device-local keys userId-scoped on both key and read path. No security-relevant surface changed in the fix pass — the PASS still holds.
- **test-engineer**: 15 PASS, 2 NOT-TESTED (AC-9 Save-never-submits, AC-17 cross-device restore), 1 PASS-with-soft-spots (AC-2 / AC-6-jump / AC-8-admin-submit-cleanup / AC-15-admin-reconnect-flip — all PASS, non-blocking tightness notes) at review time. The two NOT-TESTED ACs were flagged Critical per the report format. → **BOTH now have jest coverage**: AC-9 (`submitInventoryCount`/`submitWeeklyCount` asserted NOT called on Save) on both screens; AC-17 (fresh mount, empty local slot, restores a server-only draft — client half of cross-device visibility; pgTAP owner-read covers the DB half) on both screens. Also fixed the CI-breaking TS2556 (test-graph typecheck) it caught.
- **backend-architect** (post-impl drift): 0 Critical, 2 Should-fix, 1 Minor-accepted (M-1), minor notes. Contract landed faithfully (table/RLS/grants/upsert-mechanism/pure-module single-sourcing/staff carve-out/realtime-absence all match design byte-for-intent).
  - SF-1 (draft-load effect keyed on `isOnline` re-restores unconditionally → a realtime socket blip mid-count clobbers unsaved keystrokes; amplified on admin by the socket hook) → **FIXED**: `restoredSlotRef` restore-once-per-slot guard on both draft-load effects — a re-run syncs storage but never re-applies over in-progress typing.
  - SF-2 (design §0.2/§9 prose mislabeled the admin `useConnectionStatus` as window-online when it is realtime-socket-driven) → **FIXED in the spec** §0.2/§9.
  - M-1 (FE-dev's admin-socket-hook choice) ruled within design intent; SF-1's fix neutralizes its only downside.

## Recommended next steps (ordered)

SHIP_READY. Because the migration `20260703000000_user_count_drafts.sql` is present in the repo but **prod-apply is PENDING and user-gated**, the ship is a sequenced push-then-apply-then-verify — the same flow specs 104/105 used. `db-migrations-applied.yml` is EXPECTED to sit red in the window between push and apply; that is not a Critical, but the branch is not "healthy per both gates" until the apply lands and the gate re-runs green (CLAUDE.md CI-status rule). Order:

1. **Commit** the spec-106 change set (backend migration + pure/local/carve-out modules + db.ts helpers + both screens + i18n + all test files) on the user's explicit go-ahead. Main Claude does not auto-commit on SHIP_READY.
2. **Push to `main`.** `db-migrations-applied.yml` will go red immediately (repo migration missing from prod's `schema_migrations`) — this is the expected, transient state, not a failure to investigate.
3. **Apply the migration to prod** `ebwnovzzkwhsdxkpyjka` via Supabase MCP `execute_sql` (no `db push` — it lacks the prod password), then insert the exact `20260703000000` version into `supabase_migrations.schema_migrations`, and verify the table + RLS via the normalized-md5 check (project memory "Prod migration via Supabase MCP"). The migration is additive (new table, owner-scoped RLS, explicit grants, no data migration, no backfill, no locks beyond `CREATE`), so the apply is instant on PG 17 and non-destructive.
4. **Confirm both gates green on `main`** after the apply: `gh run list --branch main --workflow test.yml --limit 1` AND `gh run list --branch main --workflow db-migrations-applied.yml --limit 1`. Both must be green before the branch is considered healthy (`test.yml` alone is not sufficient — the 2026-06-28 lesson). Surface the run URLs.

Optional follow-ups (NOT blocking ship):
- Perform the staff Weekly interactive browser click-through when a `preview_*`-capable session is available. The admin flow was browser-verified (save → "Draft saved" + server row with client `saved_at`; navigate away → return → "Draft restored (saved Xm ago)" banner + values restored + focus on first UNCOUNTED input; Discard → cleared + server row deleted; no console errors; Save ungated at 1/143). The staff Weekly path is covered by full-render jest including the offline/reconnect paths but was not clicked through live — a low-risk gap given the two screens share the single-sourced `countDrafts.ts` reconcile/apply/serialize logic and the staff-side reconnect-flip (AC-15) is the one dedicated false→true-flip test in the suite.
- (test-engineer minor, non-blocking) A dedicated unit for the device-local trios' corrupt-bytes / malformed-shape / backup-then-remove branches (`countDraftLocal.ts` + the staff AsyncStorage half) — currently exercised only indirectly through the screen integration tests. Not an AC requirement.
- (test-engineer minor, non-blocking) A pgTAP CHECK-vocabulary-rejection arm (insert `screen = 'admin-eod'` → 23514) — neither spec 103 nor 106 covers it and it is not tied to a numbered AC.
- (code-reviewer nit, out-of-scope) The `isLocalCountDraft`/`isLocalStaffDraft` shape predicates are two hand-written copies of the same four-line check; could move to the shared pure module if a future spec touches either file. Working-as-intended carve-out duplication today.

## Out of scope for this review
- **The two EOD count surfaces** (staff EOD daily `EODCount.tsx`, admin EOD `EODCountSection.tsx`) — explicitly deferred to a follow-up spec that reuses this table. The `screen` CHECK vocabulary and table shape were kept additive so the follow-up is a CHECK-widen + nullable-`vendor_id` add (with the two-partial-index NULL-vendor pattern), never a rename.
- **Auto-save** (debounced save-on-type), **multiple named drafts per screen**, **shared/hand-off drafts**, **draft expiry/TTL sweep**, and **field-level merge / conflict-resolution UI** — all owner-decided out of v1 (OQ-1/OQ-3/OQ-5/OQ-6).
- **Client wall-clock skew under last-write-wins** — an accepted, documented v1 limitation (single-author private scratch; reconcile compares `saved_at` local-vs-server only, never against server `now()`). Must be revisited if a future spec makes drafts shared (a server-stamped monotonic sequence would replace client `saved_at`). Not a defect in this spec.
```
