# Release proposal — spec 089 (Staff Reorder page: view + cross-platform export/share)

## Verdict
verdict: SHIP_READY
rationale: Zero Critical from any reviewer, both code-reviewer Should-fixes folded in and re-verified (jest 553/553, both typechecks exit 0, live browser golden-path confirming full parity), and main's `test.yml` is green — every SHIP_READY hard rule is satisfied.

## Findings summary
- **code-reviewer:** 0 Critical / 2 Should-fix / 3 Nits. Both Should-fixes were resolved in a post-review fix-pass by main Claude (see the "## Resolution" section of code-reviewer.md): (S1) removed the dead `currentStaffUserId` Zustand subscription in `Reorder.tsx` that re-rendered on auth-user changes for zero output effect; (S2) moved the `Sharing.isAvailableAsync()` check BEFORE the temp-file write in both `nativeShare` and `shareReorderPdf`'s native branch so an unavailable share sheet no longer leaves an orphaned cache file. The extraction into `src/utils/reorderExport.ts` is byte-for-byte behavior-preserving and the HTML builder escapes every interpolated value. The 3 Nits (UTC-vs-local `todayLocalIso` naming, mount-time `maxDate` memo, untested weekday i18n keys) are cosmetic and deferred.
- **security-auditor:** 0 Critical / 0 High / 0 Medium / 1 Low. The highest-priority check — HTML injection into the `expo-print` PDF via `buildReorderPdfHtml` — PASSES: every user/catalog-controlled string is `escapeHtml`-wrapped (five-char `& < > " '`), all numeric interpolations are type-`number` AND `Number()`-coerced at the `mapReorderVendor` boundary, and a jest guard pins the escape behavior. Native temp-file write is traversal-proof (cache dir + `slugifyStore`-sanitized name + server-generated `YYYY-MM-DD` date). No cross-store/secret/PII leakage in the export; no new auth surface (read-only against the already-gated `report_reorder_list` RPC). `expo-print` is the only new dep — an Expo-maintained leaf with zero transitive packages and zero new advisories. The single Low is an informational note on a benign `console.warn`; no action required for 089. All `npm audit` findings are pre-existing Expo-toolchain transitive deps unrelated to this spec.
- **test-engineer:** 12 PASS / 0 FAIL / 3 NOT-TESTED (coverage-completeness gaps, not failures). Full suite 553/553 green (55 suites); both typechecks exit 0; the admin reorder regression gate (`ReorderSectionCases.test.tsx`) is 22/22 green through the back-compat re-export. The 3 gaps are test-completeness only — the production code is present and correct: (AC2) the `activeStore === null` fetch gate is unexercised (the tab navigator never mounts without an active store, so it's defense-in-depth), (AC5) the date-picker `onChange` re-fetch is untested at the screen layer (RPC `as_of_date` param is tested at the fetch layer), and (AC8) the `staff-reorder-loading` testID is untested (the other three states are). The named `e2e/staff-reorder.spec.ts` (Track 4) was not created — non-blocking. No backend change → no pgTAP required.
- **backend-architect (post-impl drift):** MATCHES DESIGN. 0 Critical / 0 Should-fix / 4 Minor. Confirmed NO `supabase/` change (no migration, RLS, grant, RPC, edge, or realtime delta) and that the (A) extraction is byte-for-byte behavior-preserving for the admin (seven pure formatters moved verbatim into a framework-free util; admin `ReorderSection.tsx` is import-only with a re-export keeping its jest green). Decision (C) shipped as Option 2 (PDF-everywhere via `expo-print`) — the user-resolved choice the design explicitly delegated, so it is conformance, not drift. The 4 Minors are cosmetic/QA-process notes: M1 unreachable `daysUntilNextDelivery <= 0` label (pre-existing admin behavior), M2 a manifest-vs-shipped doc note (`buildReorderPdfHtml` was planned under Option 2), M3 the web PDF success toast names a `.pdf` filename though web shows a print dialog, and M4 the native-share device-QA + EAS-rebuild ship note (carried below).

## Recommended next steps (ordered)
SHIP_READY — proceed to commit and deploy.

1. **Commit the unstaged work.** The change set covers: `src/utils/reorderExport.ts` (+ `reorderExport.test.ts`); the admin `src/screens/cmd/sections/ReorderSection.tsx` (import-only + re-export); the staff files `src/screens/staff/lib/fetchReorder.ts` (+test), `src/screens/staff/lib/shareReorder.ts` (+test), `src/screens/staff/components/ReorderDatePicker.tsx`, `src/screens/staff/screens/Reorder.tsx` (+test); `src/screens/staff/navigation/StaffStack.tsx`; the staff i18n `src/screens/staff/i18n/en.json` (+ `i18n.test.ts`); `jest.config.js`; `package.json` + `package-lock.json` (`expo-print` ~15.0.8); and `specs/089/`.
2. **Deploy.** This is FRONTEND-ONLY — there is **NO migration and NO `db push` for spec 089**. Vercel auto-deploys the web bundle on push to `main`.
3. **EAS native rebuild for the native PDF/share path.** `expo-print` is a NEW native dependency, so the native (Expo / iOS + Android) PDF + share-sheet path requires a native (EAS) rebuild to reach devices. Web is unaffected and ships via Vercel. **Native-device QA of the share path is still pending** — it is unit-tested at the orchestrator boundary but was not driven on a physical device (flagged by both the developer and the architect's M4). Run that device QA after the EAS rebuild.
4. **(Separate gate, not an 089 blocker) — the user still owes the spec-088 `db push`.** The `db-migrations-applied` gate is currently RED, but only because spec 088's migration (`20260602000000_reorder_suggested_cases.sql`) is committed to `main` yet not yet applied to prod. Spec 089 adds NO migration, so it neither causes nor worsens this. Running `npx supabase db push --linked` for 088 re-greens that gate and makes 088's case fields live in prod. This does NOT block shipping 089.
5. **Post-push CI check.** Per the project rule, after the push to `main` confirm the latest `test.yml` run is green via `gh run list --branch main --limit 1` before continuing pipeline work.

## Out of scope for this review (deferred follow-ups, non-blocking)
- **code-reviewer's 3 Nits:** the `todayLocalIso()` UTC-vs-local naming (inherited pre-extraction, unreachable in practice since the RPC always supplies `asOfDate`); the mount-time `maxDate` memo that goes one day stale only if the tab is left open past midnight; the i18n parity test exercising only 2 of 7 weekday keys (all 7 are present in `en.json` — no crash risk).
- **test-engineer's 3 coverage-completeness gaps + the absent Track-4 e2e:** the `activeStore === null` fetch-gate test (AC2), the date-picker re-fetch-on-change screen-layer test (AC5), the `staff-reorder-loading` testID test (AC8), and the named-but-absent `e2e/staff-reorder.spec.ts`. All are coverage gaps, not functional failures — the production code is present and correct.
- **backend-architect's 4 Minors:** M1 (the unreachable `daysUntilNextDelivery <= 0` cosmetic label), M2 (manifest doc note — no action), M3 (a web-specific PDF success-toast string), M4 (the EAS rebuild + native device QA — surfaced as ship step 3 above).
- **PDF-on-native was the only de-riskable item** and shipped as Option 2 per the user's choice; no further phasing remains.

## Summary
Spec 089 adds a staff-app Reorder page with full parity to the admin Reorder (calendar look-back, by-the-case display, KPI cards, per-vendor list) plus cross-platform CSV/text/PDF export, and it lands FRONTEND-ONLY with the one permitted admin touch being a byte-for-byte-preserving extraction of the pure formatters into a shared `src/utils/reorderExport.ts`. All four reviewers came back clean of Critical findings — code-reviewer's two Should-fixes were folded in and re-verified, security-auditor cleared the highest-risk HTML-injection-into-PDF path and the only new dependency, test-engineer logged 12 PASS / 0 FAIL with the admin regression green, and the architect confirmed zero backend drift — and main's `test.yml` is green, so every SHIP_READY hard rule holds. Recommendation: commit and deploy (web auto-deploys via Vercel); the only attached ship work is an EAS native rebuild plus native-device QA for the `expo-print` share path, and separately the user still owes the spec-088 `db push` to re-green the unrelated `db-migrations-applied` gate.

## Handoff
next_agent: NONE
prompt: SHIP_READY — spec 089 (Staff Reorder + cross-platform export). 0 Critical across all four reviewers; both code-reviewer Should-fixes resolved and re-verified (jest 553/553, typechecks exit 0, live browser golden-path); main test.yml green. Commit + deploy (frontend-only, NO db push for 089; Vercel auto-deploys web). Attached ship work: EAS native rebuild + native-device QA for the expo-print share path. Separately, the user still owes the spec-088 `db push` to re-green the unrelated db-migrations-applied gate (not an 089 blocker). The user decides on the commit.
payload_paths:
  - specs/089/reviews/release-proposal.md
