# Release proposal — spec 108 "Share PO" (message / WeChat share)

## Verdict
verdict: SHIP_READY
rationale: All three reviewers clean of Critical/High/Medium; both code-review should-fixes already fixed and re-verified (jest 896/896, both typechecks clean); 11/11 acceptance criteria PASS; frontend-only, no migration/edge deploy.

## Findings summary
- **code-reviewer**: 0 Critical, 2 Should-fix (BOTH FIXED post-review), 4 Nits (advisory).
  - Should-fix 1 — Share button label hardcoded `color: '#000'` instead of the palette-aware `C.accentFg` token (wrong-contrast / potentially illegible in whichever palette expects `#FFFFFF` on accent) → FIXED to `C.accentFg` (verified in tree at `POsSection.tsx:377`); the emphasis jest assertion updated to pin the token, not the old literal.
  - Should-fix 2 — the confirm→mark-sent→toast→unbusy async block duplicated verbatim between `onMarkSent` and the draft-share auto-prompt (silent-drift risk on next edit) → FIXED by extracting `runMarkSent(poId)`, now called from both sites (verified in tree at `POsSection.tsx:147`, called at `:161` and `:238`).
  - Nits (all advisory, none blocking): `poShareText.ts` docstring wording on the fallback path; `onShare` re-derives `poLines` rather than reusing the outer `lines` const; the dead `noEmailHint` key left in all three catalogs (spec D-5(c) accepted "leave it to avoid churn"); the `Sharing.isAvailableAsync()`-gates-RN-`Share` cross-subsystem check (explicitly the spec's pinned D-2 decision — reviewer flagged only so a later spec doesn't "fix" it out from under the rationale).
  - Post-fix re-verification: jest 896/896, both typechecks exit 0. Main Claude additionally browser-verified the live flow: Share renders as primary → desktop-web preview pane shows the correct template with NO `$` → honest "Did you send it?" prompt → accepting flips draft→sent (chips update) → re-share on the sent PO fires NO prompt (suppression confirmed). zh-CN body pinned by builder jest.
- **security-auditor** (LIGHT review — frontend-only, no migration/edge/RPC/RLS surface): 0 Critical, 0 High, 0 Medium, 2 Low informational. The one real security surface — a plain-text artifact leaving the app into third-party channels — is provably clean: the builder input type `PoShareLine` is type-reduced to exactly 4 safe fields (`itemId`, `itemName`, `orderedQty`, `unit`), so cost basis (`costPerUnit`), `receivedQty`, ids, vendor/manager PII, and PO uuid cannot smuggle into the body; the builder never imports `formatMoney` and its jest suite asserts no `$` in output; plain `\n`-joined text with no HTML sink (no XSS); source data is RLS-scoped Zustand slices with no fetch bypass; the status write rides spec-107's already-hardened per-store `auth_can_see_store` path. The 2 Low notes (failure-toast surfaces raw error `.message`; `console.warn` logs the error message only, never the body) are both identical to the audited `shareReorder.ts` precedent and require no change. No `package.json` change → `npm audit` correctly skipped.
- **test-engineer**: 11/11 acceptance criteria PASS (no FAIL, no NOT TESTED). Full jest 896/896 across 82 suites; base typecheck `tsc --noEmit` clean; test-graph typecheck `tsc -p tsconfig.test.json --noEmit` clean; `jest.config.js` testMatch glob extension (`src/screens/cmd/lib/**/*.test.ts`) verified live and extension-disjoint from the component project's `.tsx` glob. Two non-blocking Notes:
  - AC3 exact call-site glue (the `NameResolver` closure over `inventory` + `locale`, the `currentStore.name` / `sel.date` mapping into `buildPoShareText`) is verified by direct code trace + type-shape proof (`PoLine.itemId` = `inventory_items.id` = `InventoryItem.id`) and covered by main Claude's live browser verification of the actual rendered text — not by a section-level assertion on the built string. Reviewer's recommended follow-up: one `mockSharePurchaseOrder.mock.calls[0][0]` assertion to close the seam. Non-blocking.
  - AC5 native-primitive divergence: shipped code uses RN `Share.share({ message })` (keeping `Sharing.isAvailableAsync()` only as the availability gate) rather than the AC's literal `expo-sharing`/`Sharing.shareAsync` — a considered, documented deviation pinned in design D-2 and the Implementation notes ("Native primitive pinned"), so a PO text body doesn't leave a stray `.txt` in the vendor's chat. Resolved by design-doc authority, not a defect.
- **backend-architect**: NOT invoked — frontend-only spec with no backend contract (the spec's `## Backend design` section was authored by the architect pre-build and explicitly ships zero migrations / RPCs / edge functions / `db.ts` surface / RLS change / realtime publication). No drift surface to review.

## Recommended next steps (ordered)
SHIP_READY:
1. **Commit and push.** This spec is FRONTEND-ONLY — no prod SQL, no edge-function deploy, no migration. Single ship step: user authorizes commit → push to `main`.
2. **Post-push CI confirmation (required by the "green gates before ship" hard rule).** After the push lands, confirm the latest run of BOTH active gates on `main` is green: `.github/workflows/test.yml` (jest + both typechecks + pgTAP) AND `.github/workflows/db-migrations-applied.yml` — via `gh run list --branch main --workflow <file> --limit 1` for each, plus the e2e suite. Because this spec adds no migration and no prod SQL, `db-migrations-applied` should stay green immediately (nothing new to push to prod); `test.yml` should stay green on the already-verified jest 896/896 + clean typechecks. If either gate is red or in-progress, surface the run URL and wait for direction before treating the spec as shipped.

## Follow-ups (not blocking ship)
- (test-engineer) Add one `POsSection.test.tsx` assertion inspecting `mockSharePurchaseOrder.mock.calls[0][0]` to pin that the built text carries the inventory-resolved (not plain-English) item name — closes the AC3 call-site seam that is currently code-traced + browser-verified only.
- (code-reviewer nits) Tighten the `poShareText.ts` fallback docstring; collapse the redundant `poLines` re-derivation in `onShare`; sweep the dead `noEmailHint` i18n key in a future catalog-cleanup pass (spec D-5(c) accepted leaving it for now).

## Out of scope for this review
- **Fully-localizing decision was already resolved at build time** (main-Claude ruling): the shared body's fixed strings (header, `Store`/`Date` labels, `N items`) DO localize via an injected `labels` bundle — the one deliberate extension vs design D-4. Not a reviewer finding; noted so it isn't re-litigated.
- Everything the spec's `## Out of scope (explicitly)` block enumerates: any WeChat/SMS send API, Twilio, A2P 10DLC, Official Account/WeCom; dollar amounts in the vendor-facing text; new migrations/RPCs/edge functions; changes to spec-107's email channel or "mark as sent" behavior; automatic send-confirmation; a staff-app share affordance; PDF/CSV attach; PO create/edit changes; a reorder-card share shortcut (OQ-5 deferred to future work); `sms:`/`mailto:` deep links (`vendors.phone` captured for a future spec).
