## Verdict
verdict: SHIP_READY
rationale: Zero Critical across all four reviewers, 9/9 acceptance criteria pass with all three test tracks green, no contract drift, and the latest `test.yml` run on `main` (d7b86a1) is green — every SHIP_READY precondition holds.

## Findings summary
- **code-reviewer**: 0 Critical / 4 Should-fix / 6 Nits. Top issues: (1) `unitConversion.ts:207-210` JSDoc still describes the repudiated `caseQty × subUnitSize` formula for `convertToItemUnit`'s Case-3 path — a stale spec comment one git-blame from re-introducing the 12× error (impl at :244-254 is correct); (2) `scripts/smoke-migrate-spec093.sh:33` uses `set -u` only — add `set -o pipefail` or an explicit comment so a piped `docker exec` failure isn't silently swallowed; (3) `IngredientForm.tsx:802` readback `contentsUnit = subUnitUnit || unit || 'each'` derives the post-"=" noun from the tracking unit (`unit`) — per-spec but the variable name misleads; a one-line comment would help; (4) `unitConversion.ts:290-297` `const totalPerCase = caseQty;` is now a single-value alias with no computational purpose — inline it. All four are advisory; none affect behavior.
- **security-auditor**: PASS — 0 Critical / 0 Should-fix / 0 Nits-with-security-impact. Every focus item clear: new `spec093_case_qty_backfill_audit` table is RLS-enabled + `revoke all` from anon/authenticated (deny-all back-office posture, not in any realtime publication, no spec-053 lint or spec-065 anon-grant regression); backfill UPDATE is 100% static SQL (no injection surface) and only mutates Population B; form/cost-calc changes are arithmetic-only with no new input-validation gap, no secret exposure, no dependency changes.
- **test-engineer**: 9/9 acceptance criteria covered and PASS. All three tracks green — jest 57 suites / 578 tests, pgTAP 43/43 (spec093 backfill 8/8 incl. reorder round-trip `ceil(50/20)=3`; permissive lint stays 4/4), Track-3 smoke clean (0 mis-encoded rows post-backfill, idempotent re-apply, 42 Population-C rows flagged). Documented intentional gaps (all owner-accepted, none blocking): AC2/AC3 verify form-state → `toUpdates()` chain as pure-function tests rather than a real DB read-back (db.ts:278-280 is the unchanged correct seam, and the Track-3 smoke confirms 0 mis-encoded live rows); R2 (`default_cost` recompute) and R4 (`calcCasePrice` asymmetry) are out-of-scope per owner and carry no tests by design.
- **backend-architect** (post-impl): no drift — 0 Critical / 0 Should-fix / 3 Nits (all documentation/wording). Migration matches §1a/§1b predicates and audit-table shape exactly; `calcUnitCost` + db.ts:3710 fallback divide by `case_qty` alone per §5/§8; form re-bind/relabel/readback matches §7/§9; db.ts:278-280 unchanged. Owner-confirmed boundaries (R2/R4/db.ts:278-280) all held. The realtime-publication-snapshot gotcha explicitly does NOT apply (no publication-membership change). Nits: (1) audit table has no success-path lifecycle owner and will persist in prod after the push; (2) R4's practical blast radius is zero today — neither `calcUnitCost` nor `calcCasePrice` is called anywhere in non-test app code; (3) readback noun is hardcoded `1 case = …` regardless of DEFAULT UNIT, but AC-compliant.

Cross-cutting confirmation: main Claude browser-verified the fix on the real web build — the edit drawer renders `UNITS / CASE` + `SUB-UNIT / UNIT` labels with reconciled help, and the readback shows `1 case = 450 each`, updating live to `1 case = 20 each` when the case-size field changed. Confirmed behavior, not just typecheck.

## Recommended next steps (ordered)

SHIP_READY.

1. **Commit and deploy.** Stage the working-tree changes (backend slice: migration `20260602120000_spec093_case_qty_backfill.sql`, `unitConversion.ts`, `db.ts`, pgTAP, smoke, jest pins; frontend slice: `IngredientForm.tsx`, `IngredientForm.help-text.test.tsx`, `IngredientForm.spec093.test.tsx`, `EODCount.test.tsx`) and commit per the user's go-ahead. Nothing is committed yet.

2. **REQUIRED post-merge action (owner-run, not optional):** After the commit lands, the owner MUST run `supabase db push` to apply `20260602120000_spec093_case_qty_backfill.sql` to prod. This is the intended owner-run posture, NOT a defect — but it is load-bearing twice over:
   - It is the only thing that actually fixes the live prod rows (a local `db reset` runs migrations BEFORE seed, so the migration sees an empty catalog locally; the Track-3 smoke is what exercises it against seeded/prod-shaped data).
   - The `db-migrations-applied.yml` drift gate will hard-fail on the NEXT CI run if prod's `schema_migrations` is missing the `20260602120000` entry. Do not let the push slip between SHIP_READY and the next push to `main`.
   After the push, the owner hand-reviews the Population-C split rows: `select * from public.spec093_case_qty_backfill_audit where population = 'C';` (~42 flagged rows in the current seed mirror) — these are intentionally NOT auto-mutated.

3. **(Optional, non-blocking — may be folded in before committing.)** Severity-ranked optional fix-list. None of these block ship; all are Should-fix or Nit and the build is green with them present:
   1. *(Should-fix — do first; highest re-regression risk)* `src/utils/unitConversion.ts:207-210` — update the `convertToItemUnit` JSDoc "Resolution order" line so it no longer describes the repudiated `caseQty × subUnitSize` Case-3 formula. The impl is already correct; this is a stale specification comment that could mislead a future reader straight back into the 12× error. Lowest-risk, highest-value of the four.
   2. *(Should-fix)* `scripts/smoke-migrate-spec093.sh:33` — add `set -o pipefail` (matching `smoke-multi-brand.sh`) OR an explicit comment documenting why `-e` is omitted (matching `smoke-rpc.sh:30`), so a `docker exec` failure inside `$(...)` isn't silently swallowed.
   3. *(Should-fix)* `src/utils/unitConversion.ts:290-297` — inline the now-vestigial `const totalPerCase = caseQty;` alias to `if (caseQty <= 0) return 0; return casePrice / caseQty;`, matching the db.ts:3719 fallback shape. Pure readability; behavior is correct.
   4. *(Should-fix)* `src/components/cmd/IngredientForm.tsx:802` — add a one-line comment distinguishing the two branches of `contentsUnit = subUnitUnit || unit || 'each'` (why the DEFAULT UNIT / tracking unit is the fallback), since the `contentsUnit` name reads as "what comes after =" but is derived from the tracking unit.
   5. *(Nit — cross-cutting copy)* `PACK_UNIT_HELP` grammar: the help string `"… each, lb, oz; For abstract units …"` uses a semicolon followed by a capitalized `For`. Fixing it requires editing BOTH the component source (`IngredientForm.tsx`) and the pinned constant in `IngredientForm.help-text.test.tsx:127` in the same change so the test stays green. Pure prose; no behavior impact.

## Out of scope for this review
- **`default_cost` value recompute (R2).** Owner-confirmed out of scope (spec §Out-of-scope). Migrated Population-B rows will have `default_cost` internally inconsistent with `default_case_price / case_qty` until a future spec — but the app prefers the per-store `inventory_items.cost_per_unit`, so the fallback rarely fires. Flagged by backend-architect §10-R2 as the most likely "why is the cost wrong?" post-deploy surprise; belongs in its own data-migration spec, not 093.
- **`calcCasePrice` asymmetry (R4).** `calcCasePrice` still multiplies `× subUnitSize` while `calcUnitCost` no longer does. Owner-confirmed out of scope; backend-architect confirms zero runtime blast radius today (neither function has a non-test call site). If a future spec wires `calcCasePrice` into a real cost→price round-trip, R4 re-arms and should be revisited there.
- **Audit-table lifecycle cleanup.** `public.spec093_case_qty_backfill_audit` is intentionally not dropped on the success path (it is the backout source + the Population-C hand-review list). Once the owner resolves the C rows, a tiny follow-up cleanup migration to drop the spec-named one-shot table would prevent permanent schema cruft (backend-architect Nit-1). Separate follow-up, not a 093 blocker.
- **Readback noun for non-"case" pack nouns** (e.g. DEFAULT UNIT=`tray`). The readback hardcodes `1 case = …`; §9 explicitly left the noun source to FE discretion and no AC pins the non-"case" shape. Acceptable as shipped (backend-architect Nit-3); a future copy-polish spec could derive the noun if desired.

## Handoff
next_agent: NONE
prompt: SHIP_READY
payload_paths:
  - specs/093/reviews/release-proposal.md
