## Test report for spec 158

### Acceptance criteria status

- AC-1 (17 admin topics in sidebar order + correct gates; 5 staff topics) → PASS — `src/lib/guide.test.ts::guideTopics — AC-1` (exact array-equality pins, gate assertions, staff 5-topic pin)
- AC-2 (compile-time drift gate: `Record<Exclude<AdminSectionId, GuideExemptSectionId>, GuideTopic>`, node-project-only imports) → PASS — `src/lib/guide.test.ts::AC-2` (`@ts-expect-error` pin, run for real by `npm run typecheck:test`, confirmed clean) + manual falsification (below) + `guide.ts` has zero `react-native` import (stricter than "beyond Platform")
- AC-3 (nav-parity test across plain admin / master / super_admin, both directions) → PASS — `src/screens/cmd/__tests__/navGuideParity.test.tsx::AC-3`
- AC-4 (Guide item in a Help group, last, at all three breakpoints; renders `cmd-guide-section`) → PASS — `navGuideParity.test.tsx::AC-4` (group/order across 3 roles) + `src/screens/cmd/sections/GuideSection.test.tsx` (`cmd-guide-section` renders)
- AC-5 (index rows `cmd-guide-index-<id>` in sidebar order; body `cmd-guide-topic-<id>` with title/purpose/bullet-count == `actions`) → PASS — `GuideSection.test.tsx` ("rows render in sidebar order", "bullet count equals `actions`")
- AC-6 (phone: PhoneDrillScaffold list→detail, scroll survives, no clipping) → PASS — `GuideSection.test.tsx::AC-6` (list stays mounted under detail, back returns to list). Horizontal-clipping claim is manual-only (see Notes; consistent with the spec's own "Manual check (not CI)" carve-out for 375px overflow).
- AC-7 (`?` in TitleBar cluster desktop+tablet, opens `GuideSheet` on active section without changing it) → PASS — `ResponsiveCmdShell.spec158.test.tsx::AC-7` (desktop/tablet/phone existence + press→open+correct topic + section unchanged) + `GuideSheet.test.tsx::AC-7`. Note: the shell test mocks the real `TitleBar`/`MobileTopAppBar` down to the prop they receive, so the wiring is pinned but the *exact* real-`TitleBar` JSX (testID position "before the bell", a11y label) is not independently unit-rendered — see Notes. AC-7's "glyph twin in collapsed tablet rail" clause was struck by architect correction C-2 and is explicitly asserted absent (exactly one `cmd-guide-entry`) in the same suite.
- AC-8 (phone: pressable title + always-visible `?`, bar stays 52px, trailing cluster gains no control, no overflow/truncation below one legible word at 375px w/ super-admin) → PASS for the structural/height/no-new-trailing-control claims — `MobileTopAppBar.test.tsx` (52px pin, always-visible marker, ≥44 target) + `ResponsiveCmdShell.spec158.test.tsx` ("phone puts it on the TITLE, not the trailing cluster"). The 375px-with-brand-picker visual non-overflow claim is manual-only per the spec's own verification plan (jest cannot lay out real pixel widths) — see Notes.
- AC-9 ("See all pages" switches to index; index rows open in place; closing returns to exact prior screen/state) → PASS — `GuideSheet.test.tsx::AC-9` + re-seed tests. "Closing returns to exact screen/state" follows structurally from `onClose` only flipping a local `guideSheetOpen` boolean (verified in the `ResponsiveCmdShell.tsx` diff) — no other state is touched.
- AC-10 (drawer closed in the same handler before the sheet opens; never two live Modals) → PASS — `ResponsiveCmdShell.spec158.test.tsx::AC-10`
- AC-11 (no file under `src/screens/cmd/sections/` other than the new `GuideSection.tsx` modified; `?` derives topic from shell `section` state) → PASS — `git status` shows only `GuideSection.tsx` (+ its test) added under that directory, zero other files in it touched; `GuideSheet` is fed `topicId={guideSheetTopicId}` seeded from `section` in `ResponsiveCmdShell.tsx`, confirmed no per-section wiring added anywhere else
- AC-12 (staff `Guide` screen, `topicId` param, index vs. topic, back-to-index, hardware back to caller) → PASS — `src/screens/staff/screens/Guide.test.tsx::AC-12`
- AC-13 (`HelpButton` in 5 staff screen headers, own topic id each, ≥44×44 target, doesn't displace neighbors) → PASS — `src/screens/staff/components/HelpButton.test.tsx` + presence confirmed in all 5 screens (`EODCount.tsx`, `Reorder.tsx`, `WeeklyCount.tsx`, `StorePicker.tsx`, `Settings.tsx`) via grep + `StorePicker.test.tsx` "does not displace the title or store rows" pin. The ≥44×44 claim is met via `hitSlop` (flagged deviation #2, accepted — token-consistent with `SettingsGear`), pinned in `HelpButton.test.tsx`.
- AC-14 (staff Settings Guide row between Text size and install-guide card, opens index) → PASS — `Settings.test.tsx::"Settings — Guide row (Spec 158)"` (both the navigate-with-no-topicId assertion and the ordering pin)
- AC-15 (staff reaches only staff topics: no admin string in staff catalogs, no staff file imports admin registry) → PASS — `src/lib/guide.i18n.test.ts::AC-15` (catalog subtree equality, cross-catalog leak grep, purpose-paragraph leak grep, forbidden-import file-walk grep, literal-`'staff'`-audience grep) + `Guide.test.tsx::AC-15` (admin ids fall back to index at runtime)
- AC-16 (every `guideTopicKeys()` key resolves in all 3 catalogs per surface) → PASS — `guide.i18n.test.ts::AC-16` for both surfaces, all 3 locales each, non-empty-string assertion
- AC-17 (zero new assets/deps; `package.json` unchanged) → PASS — `git status` shows no additions under `assets/`/`public/`; `git diff --cached --stat package.json` is empty
- AC-18 (zero backend surface: no `supabase/**`, no `db.ts`/`useStore.ts`/`useStaffStore.ts`, no network call, `app.json` untouched) → PASS — `git diff --cached --stat` empty for all six named files/dirs; grep for `supabase.from|supabase.rpc|supabase.functions.invoke|fetch(` and `auth_can_see_store` across every new/modified spec-158 file returns zero hits. The staged `supabase/migrations/20260809000000_super_admin_policy_parity.sql`, `supabase/tests/super_admin_policy_parity.test.sql`, `src/screens/DBInspectorScreen.tsx` and its new test are confirmed **pre-existing spec-157 work** (content is `auth_is_privileged()` RLS parity, zero mention of "guide" anywhere in the diff) — correctly out of scope for this review per the task instructions.

### Regression group (AC-REG)

- AC-REG1 (type-only `NavItem` adoption: rendered groups/ids/labels/order/gates/dispatch unchanged) → PASS — `navGuideParity.test.tsx::"AC-REG1: the pre-existing groups are behaviorally unchanged"` pins the full pre-158 id sequence plus the appended `Guide`; full jest green (no sidebar/shell suite broke)
- AC-REG2 (saved `profiles.sidebar_layout` override still shows every previously-visible item in its saved position, plus `Guide` in default position; no `sidebarLayout.ts` change) → **PARTIAL / gap.** The "no `sidebarLayout.ts` change" half is verified directly (`git diff --cached --stat src/lib/sidebarLayout.ts` is empty). The **behavioral** half — that a user with a saved override actually sees `Guide` appended via `applySidebarOverride`'s default-group fallback — is **not exercised by any test**, old or new. The only real (non-mocked) exercise of `applySidebarOverride` is `src/screens/cmd/sections/__tests__/OrderingSection.test.tsx`, which covers the spec-137/138 `Ordering`/`Reorder`/`PurchaseOrders` remap only, never `Guide`. Both spec-158 shell test files (`ResponsiveCmdShell.spec158.test.tsx`, `.spec153.test.tsx`) mock `applySidebarOverride` as identity, so they don't exercise the merge either. This is a **pre-existing test-coverage gap** (spec 060 added `MenuImpact` via the identical mechanism and it appears equally untested), not a spec-158-introduced regression, and the code path itself is provably unchanged — but as written, AC-REG2's behavioral claim has no automated pin. Recommend a follow-up unit test on `applySidebarOverride` directly (call it with a synthetic saved override missing `Guide`, assert `Guide` lands in the Help group at its default position) rather than blocking release on it, given the mechanism is identical to the already-shipped spec-060 precedent.
- AC-REG3 (spec-153 chrome unchanged; `TitleBar`/`MobileTopAppBar` byte-identical with no new prop) → PASS — `MobileTopAppBar.test.tsx::"AC-REG3: no onTitlePress → plain title"`, `ResponsiveCmdShell.spec158.test.tsx::"AC-REG3 — spec-153 chrome is untouched"` (install chip + rail twin still render); `TitleBar.tsx` diff shows the new control is 100% gated behind the optional `onHelpPress` prop
- AC-REG4 (staff EOD/offline-queue path, spec-126 Settings order incl. `[Guide]`, spec-152 sign-out ordering pins) → PASS — `Settings.test.tsx` order pin (Notifications → Language → Text size → **Guide** → install guide → Report an issue → Sign out) plus the pre-existing spec-152 `markIntentionalSignOut()`-before-`signOut()` tests are present, unmodified, and green in the full run

### Test run

- `npx jest --listTests | grep -i -e guide -e navGuide`: all new suites discovered — `src/lib/guide.i18n.test.ts`, `src/components/cmd/GuideSheet.test.tsx`, `src/screens/cmd/sections/GuideSection.test.tsx`, `src/screens/staff/screens/Guide.test.tsx`, `src/screens/cmd/__tests__/navGuideParity.test.tsx`, `src/lib/guide.test.ts` (plus, confirmed separately since they don't match `-e guide`/`navGuide` literally by filename: `src/screens/cmd/__tests__/ResponsiveCmdShell.spec158.test.tsx`, `src/screens/staff/components/HelpButton.test.tsx`). No repeat of the C-1 "test that never runs" failure mode.
- Full `npx jest`: **210 suites / 2325 tests, all green** — matches the developer's self-reported numbers exactly.
- `npx tsc --noEmit`: clean (exit 0).
- `npm run typecheck:test`: clean (exit 0).
- **Falsification of the compile-time drift gate (performed and reverted):** added a fake sidebar item `{ id: 'FakeTestSection', label: 'fake' }` to the Operations group's `NavItem[]` array in `src/lib/cmdSelectors.ts`, then re-ran `npx tsc --noEmit`:
  ```
  src/lib/cmdSelectors.ts(1123,13): error TS2322: Type '"FakeTestSection"' is not assignable to
  type '"Inventory" | "Dashboard" | "Ordering" | ... | "Users"'.
  ```
  Confirms the Layer-1 compile gate genuinely fires at the `NavItem` seam (a fake id is rejected before the guide `Record` is ever reached). Edit reverted immediately after; `git diff --cached --stat src/lib/cmdSelectors.ts` and `npx tsc --noEmit` confirmed clean afterward.

### Notes

- **AC-REG2 gap (see above).** Not blocking on its own — the code path is unchanged and mirrors an already-shipped precedent (spec 060) — but genuinely untested. Recommend a follow-up `sidebarLayout.test.ts` covering the general fallback mechanism, not just the spec-137/138 remap case it happens to be exercised through today.
- **TitleBar real-component render gap.** AC-7's core behavior (existence, opens sheet, correct topic, section unchanged, exactly one `cmd-guide-entry` including the collapsed-tablet C-2 case) is thoroughly pinned in `ResponsiveCmdShell.spec158.test.tsx`, but that suite mocks `TitleBar` down to the `onHelpPress` prop it receives — there is no `TitleBar.test.tsx` addition that renders the real component with `onHelpPress` to independently verify the JSX position ("before the bell"), the `cmd-guide-entry` testID, or the accessibility label on the actual component. The source read confirms correct placement and the developer's manual browser pass claims to have exercised it live. Low severity — flagging for completeness, not blocking.
- **AC-6 / AC-8 pixel-level overflow claims are manual-only,** consistent with the spec's own "Manual check (not CI)" section (375px real-phone pass, es/zh-CN longest-label pass). jest/jsdom cannot assert real flex-wrap/text-overflow at a fixed viewport width, so this is an accepted, spec-acknowledged gap rather than a developer omission.
- **Falsification and manual sabotage were performed only on `src/lib/cmdSelectors.ts` (pure TS, no UI surface) and reverted before this report was written; no lasting diff.**
- **Zero backend surface confirmed independently**, not just by trusting the self-report: `git diff --cached --stat` was run per-file against the full "explicitly not in the diff" list from §12/§13 of the spec (`supabase/**` carve-out aside, `src/lib/db.ts`, `src/store/useStore.ts`, `src/screens/staff/store/useStaffStore.ts`, `src/lib/sidebarLayout.ts`, `jest.config.js`, `package.json`, `app.json`, `vercel.json`, `eas.json`, `CLAUDE.md`) — all empty. The two staged `supabase/**` files and the `DBInspectorScreen.tsx`/test pair belong to spec 157 (verified by reading their diff content: `auth_is_privileged()` RLS parity work, zero "guide" mentions) and are correctly excluded from this spec's scope per the task framing.
- **Deviations section (developer's own list) reviewed against the diff and found accurate**: `guide.back` chrome key is present in `GUIDE_CHROME_KEYS` and all six catalogs and is exercised by the R-9 parity test; the `hitSlop`-based ≥44×44 approach for `HelpButton` is pinned in its own test; `PhoneDrillScaffold`/`usePhoneDrill` are read-only imports (no existing section file modified, confirmed via `git status`); `visibleGuideTopics()` exists in `guide.ts` and is covered by `guide.test.ts`; `ADMIN_SECTION_ORDER` is pinned against `ADMIN_SECTION_IDS` in `guide.test.ts::"the admin index order IS ADMIN_SECTION_IDS minus the exempt ids"`; the reserved `overview` group ships unused, asserted by `guide.test.ts::"OQ-1 is NOT built"`.
- No pgTAP or shell-smoke coverage needed or attempted — correctly, per the spec's own "No pgTAP track... No shell-smoke track" line; this is a frontend-only, zero-backend feature.
