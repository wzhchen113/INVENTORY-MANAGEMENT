## Verdict
verdict: SHIP_READY
rationale: All four reviewers finished at 0 Critical, every Should-fix/minor is resolved with a source-verified artifact, and post-fix gates are green (jest 950/950, both typechecks 0, pgTAP 63/63) — the only remaining step is the user-gated prod migration apply + commit.

## Findings summary
- **code-reviewer:** 0 Critical, 2 Should-fix, 3 Nits — ALL RESOLVED (Nit 3 correctly NO-ACTION per house style). Top issue: SF-1 category-filtered drag silently narrowed the persisted store-shared layout (stakes raised vs spec 103 because the artifact is now shared, not per-user private). Fixed via option (a) — the Custom-view drag list (web @dnd-kit + native ▲/▼ movers, both behind the same conditional) now disables when `selectedCategory !== 'all'`, matching the existing search guard. Verified in source at `src/screens/cmd/sections/InventoryCountSection.tsx:1727` (`search.trim() || selectedCategory !== 'all'`) + a new jest pin drives the native branch. SF-2 no-op `setLayouts(prevLayouts)` in `persistLayout` removed (failure path is now a bare `return`, lines 465-470; the two surviving reverts at 530/563 are the rename path's REAL optimistic revert, untouched). Nit 1 fifteen dead i18n entries trimmed ×6 catalogs; Nit 2 shared `blockIfAtCap()` helper (lines 486-492, used by both create paths).
- **security-auditor:** 0 Critical, 0 High, 1 Should-fix, 2 Nits — SF-1 RESOLVED; N-1/N-2 correctly waived. All 8 load-bearing claims exercised against the live local stack (not inferred from SQL). SF-1: rename/delete RPCs leaked a bounded layout-id existence oracle (role gate ran AFTER the not-found check, so a non-privileged cross-store caller could distinguish a real id (42501) from a fake one (P0002)). Fixed by moving the `auth_is_privileged()` gate ahead of the row-resolve — verified in source at `supabase/migrations/20260706000000_store_count_layouts.sql:319-330` (rename: role gate step (1) at 319-321, row-resolve step (2) at 325-330), matching the `demote_profile_to_user` reference shape. Three new pgTAP pins (18b/c/d, `store_count_layouts.test.sql:385-405`) assert REAL id and FAKE id both → SAME 42501 for rename and delete; `plan(27)`→`plan(30)`. N-1 (`service_role` EXECUTE) and N-2 (TRUNCATE grant) are project-wide `ALTER DEFAULT PRIVILEGES` defaults identical to sibling tables, unreachable (both fail-close on `auth.uid() = null` / no PostgREST verb) — reasonable waivers.
- **test-engineer:** 13 acceptance criteria, 13 PASS / 0 FAIL / 0 NOT TESTED. Every AC independently re-verified (own suite run, not trusted counts). AC-3b (the headline privileged-only server-side write gate) is triple-covered: RPC role-gate 42501 (case 15), direct-INSERT RLS 42501 (case 16), direct UPDATE/DELETE 0-rows (17/18). Two non-blocking minors from the review BOTH landed: a "Save as new" press-test (asserts `layoutId null` create even with a layout selected) and the modal backdrop a11y label now uses `common.close` (translated ×3). Coverage boundaries disclosed honestly and reasonably: "appears on both surfaces" is a structural consequence of the shared table + shared RLS SELECT (source-confirmed identical query on both paths), native (EAS) half of AC-8 is unverified by automated test (pre-existing repo-wide gap, not spec-110-specific).
- **backend-architect (post-impl drift):** 0 Critical, 0 Should-fix, 0 Minor drift. Implementation matches `## Backend design` §0-§12 decision-for-decision (table shape + position-slot semantics + CHECKs + `unique(store_id, position)`; RPC trio gate order + advisory lock + P0001/42501/P0002 codes + byte-identical refusal strings; four RLS policies; bounded AC-13 DELETE; no realtime membership; read-only staff carve-out; §6 camelCase mapping). Migration-version correction to `20260706000000` ruled WITHIN DESIGN (`20260704000000` and `20260705000000` both taken by intervening migrations). Two within-design deltas are defensive supersets (NULL `item_ids`/`p_name` coalesced to the same error), not divergence. Two INFORMATIONAL notes (not findings) folded into the ship gates below.

## Recommended next steps (ordered)

SHIP_READY — but two user-gated steps precede the "done" state (work is uncommitted; prod is not yet migrated):

1. **Apply the migration to prod, then record the version.** `supabase/migrations/20260706000000_store_count_layouts.sql` is applied to the LOCAL stack only. Apply to prod via the Supabase MCP (`db push` lacks the prod password — project memory "Prod migration via Supabase MCP"), then insert version **`20260706000000`** into `supabase_migrations.schema_migrations` so `db-migrations-applied.yml` (spec 064) stays green. NOTE the version: one stale `20260704000000` string survives in the spec §12 PROSE only (architect Note 1) — it is NOT in any artifact; the migration on disk, its header, and the Files-changed block all correctly say `20260706000000`. Record `20260706000000`.
2. **Run the BROADER §12 post-apply verification (not a body-only apply).** This migration ships table + 4 RLS policies + 3 SECURITY DEFINER RPCs + explicit grants + the AC-13 cleanup DELETE, so the post-apply check is wider than a body-only RPC re-apply: confirm the table/columns + CHECKs + `store_count_layouts_store_position_uq`, all four policy predicates (`auth_can_see_store` SELECT; `auth_is_privileged() AND auth_can_see_store` on I/U/D), the three RPC signatures + SECURITY DEFINER + `search_path` + EXECUTE-to-authenticated-only, and that the AC-13 `DELETE ... WHERE screen IN ('admin-inventory','staff-weekly')` landed while the two EOD families survived (capture the EOD row count BEFORE apply for the comparison). The spec's §12 checklist enumerates these.
3. **Confirm both CI gates are green on `main` after the migration push.** Per the CLAUDE.md CI-status hard rule: `test.yml` AND `db-migrations-applied.yml` must both show a green latest run on `main` before this is truly shipped — the `db-migrations-applied` gate is the one that hard-fails on a repo migration missing from prod, so it is the direct signal step 1 landed. CI on `main` was green as of the last push; re-confirm after the prod apply.
4. **Commit** — the user gates the commit (work is currently uncommitted; main Claude does not auto-commit on SHIP_READY).
5. (optional, non-blocking follow-ups — none required for ship) Nothing outstanding: the two test-engineer minors and both code-reviewer/security Should-fixes are already resolved in this change set.

## Out of scope for this review
- **N-1 / N-2 grant posture (schema-wide, not spec-110):** making the "TRUNCATE deliberately omitted" comment true, or stripping `service_role` EXECUTE to exactly match `demote_profile_to_user`, are schema-wide `ALTER DEFAULT PRIVILEGES` / `REVOKE` decisions affecting every `public.*` table — a separate hygiene spec, not this one. Both are unreachable as vulnerabilities here (null-caller fail-close + no PostgREST TRUNCATE verb).
- **Native (EAS) automated test harness (AC-8's native half):** the repo has no native-specific jest project; AC-8's "works on native" is verified only by RNW/jsdom + the browser pass, per the standing CLAUDE.md native-testing gap. Establishing native test infra is a cross-cutting follow-up, not spec 110.
- **`customOrder.test.tsx` hand-mirrored logic drift risk (design note):** the pre-existing spec-103 pattern of re-implementing `deriveFilteredItems`/`buildEntrySet` as standalone doubles could silently rot if the real component drifts. Flagged by test-engineer for future-spec awareness; predates spec 110, not a regression to fix here.
- **Server-enforced per-store layout name uniqueness (OQ-A):** deliberately NOT enforced (two "Walk A" layouts are harmless, picked by id) so overwrite-vs-new stays last-write-wins instead of throwing an opaque 23505. If the owner later wants a unique `(store_id, name)`, that is a new decision.

## Handoff
next_agent: NONE
prompt: SHIP_READY
payload_paths:
  - specs/110-named-weekly-count-layouts/reviews/release-proposal.md
