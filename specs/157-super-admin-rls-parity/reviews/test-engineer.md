## Test report for spec 157

### Acceptance criteria status

**A. Policy parity (the seven)**

- AC-1 (`ingredient_categories` writes) → PASS — `supabase/tests/super_admin_policy_parity.test.sql::IC-1/IC-2/IC-3` (super_admin INSERT/UPDATE/DELETE), `::IC-4/IC-5/IC-6` (plain-user negative). Live re-check: `pg_policies` predicates for all three write policies now read `auth_is_privileged()`.
- AC-2 (`audit_log` UPDATE/DELETE) → PASS — `::AL-5/AL-6` (super_admin UPDATE/DELETE a store-scoped row, 1 row affected). Note: `deleteStore`'s cascade and `cleanupOldRecords`' purge are not exercised end-to-end through `db.ts` (out of scope per the spec's `src/lib/db.ts` read-only carve-out) — the RLS predicate both paths depend on is what's pinned, which is the correct unit of test given no `db.ts` change shipped.
- AC-3 (`audit_log` store-less rows) → PASS — `::AL-1/AL-2` (super_admin INSERT+SELECT store-less row), `::AL-3/AL-4` (plain-user negative). Byte-unchanged `store_id IS NOT NULL` arm confirmed by reading the migration diff against `20260504173035_per_store_rls_hardening.sql:160-181`.
- AC-4 (strict superset) → PASS — `::IC-7/IC-8` (admin/master JWT INSERT still succeeds), `::AL-9/AL-10` (admin/master JWT UPDATE/DELETE still succeed).
- AC-5 (`auth_is_admin()` unchanged) → PASS — live `pg_get_functiondef('public.auth_is_admin()')` re-run by this review: body is still the two-line JWT-only `select … = any(array['admin','master'])`, unchanged shape.
- AC-6 (one migration, idempotent) → PASS — exactly one new file (`20260809000000_super_admin_policy_parity.sql`); this review independently re-applied the full migration body a second time via `psql` against the local stack and it completed clean (all `DROP POLICY`/`CREATE POLICY`/`COMMENT`/`CREATE FUNCTION` succeeded with no error), then re-ran the full pgTAP suite — still 81/81, confirming the re-apply didn't corrupt policy state.
- AC-7 (applied to prod, gate re-green) → **NOT TESTED / NOT YET DONE.** The spec's own "Still owed before this can ship" section (line ~1622) states the prod apply step was deliberately not performed by the implementer, and the D-0 record at the bottom of the spec only ran the **read-only verification queries** via Supabase MCP (confirming 7 rows in prod `pg_policies`, 4 rows in the function-body query, 0 rows in the gate-only probe, `audit_log` count 4,485) — it did **not** run `execute_sql` to apply the migration body, insert the `schema_migrations` row, or re-check `db-migrations-applied.yml`. Nothing has been committed or pushed to `main` yet (`git log` shows no new commit for this spec). This AC cannot be marked PASS until the prod apply + gate re-check actually happens.
- AC-8 (permissive-policy lint stays green, no allowlist row) → PASS — `supabase/tests/permissive_policy_lint.test.sql` passed (4/4 assertions) in the same `npm run test:db` run; `git diff --stat` confirms that file is untouched (not in the 5-file diff).
- AC-9 (rule written down in-database) → PASS — live `obj_description('public.auth_is_admin()'::regprocedure, 'pg_proc')` re-checked by this review; contains all four required clauses (narrow tier, super_admin exclusion + reason, "gate on auth_is_privileged()" instruction, deliberate-exclusion escape hatch) plus the spec-157 pointer and the `user_count_drafts`/`user_count_orders` counter-example.

**B. Honest DB Inspector auth banner**

- AC-10 (truthful predicate) → PASS — `src/screens/__tests__/DBInspectorScreen.test.tsx` arms 1 & 2 (`classifyAuthBanner` returns `privileged-jwt` / `privileged-super-admin`, both tone `ok`).
- AC-11 (probe gains `is_privileged`/`is_super_admin`, `is_admin` kept, other keys unchanged) → PASS — live SQL re-check by this review confirms `admin_db_inspector_probe()` body contains `is_privileged`, `is_super_admin`, `is_admin`, and `prosecdef = true`; reading the migration diff confirms the `schema`/`counts`/`recipe_groups`/`prep_groups` blocks are byte-identical to `20260517020000_admin_rpcs_use_privileged.sql:14-135` (only the `auth` jsonb_build_object block differs).
- AC-12 (copy names only what's gated) → PASS — jest arm 2 asserts no `NOT admin per the JWT` / no `ALL your CRUD` and asserts the `profiles.role`/`super_admin` explanation is present; arm 6 (whole-map scan) asserts `ALL your CRUD` appears in **no** `AUTH_BANNER_COPY` entry.
- AC-13 (impossible state labelled as such) → PASS — jest arm 3: `not-privileged` copy contains `should not have returned` and `admin_db_inspector_probe`, tone `warn` (not treated as a routine diagnosis).
- AC-14 (no i18n surface added) → PASS — `grep -n "useT|i18n" src/screens/DBInspectorScreen.tsx` returns nothing; no `src/i18n/*` file in the diff.
- AC-15 (payload-shape resilience) → PASS — jest arm 4 (fields absent → `unknown`, tone neither `ok` nor `warn`), arm 5 (`null`/`undefined` → `unknown`), plus a bonus arm (non-boolean `is_privileged` treated as absent, not `false`).

**C. Regression group (AC-REG)**

- AC-REG-1 (no policy other than the seven changes) → PASS — this review independently re-ran `select … from pg_policies where qual/with_check like '%auth_is_admin%'` against the local stack post-apply: **0 rows**, matching the seven expected rewrites (down from the pre-migration 7).
- AC-REG-2 (helpers frozen) → PASS — `auth_is_admin()` body verified unchanged (AC-5). `auth_is_super_admin()`, `auth_is_privileged()`, `auth_can_see_store()`, `auth_can_see_brand()` are not referenced by any statement in the new migration file besides being *called* inside the rewritten policies/probe (not redefined); `git diff --stat` confirms no other migration file is touched, so their bodies are provably unchanged.
- AC-REG-3 (no scope loosening beyond role) → PASS — verified by reading the migration text: every `store_id IS NOT NULL AND auth_can_see_store(store_id)` OR-arm is byte-identical to the pre-change source; `admin_update_audit_log`/`admin_delete_audit_log` remain unscoped by store (explicitly, with a `comment on policy` recording the deferred gap per OQ-3).
- AC-REG-4 (existing pgTAP suites green, unmodified) → PASS — all seven named suites (`recipe_categories_super_admin_rls`, `legacy_permissive_policy_dropout`, `permissive_policy_lint`, `rls_hardening_followups`, `admin_rpcs_privileged`, `actor_fk_cascade_audit`, `missed_order_audit_rpc`) appear green in the `npm run test:db` output, and `git diff --stat` confirms none of the seven files is in this spec's diff (only the new `super_admin_policy_parity.test.sql` was added).
- AC-REG-5 (staff untouched) → PASS — `git diff --stat` confirms nothing under `src/screens/staff/` or `src/i18n/*`.
- AC-REG-6 (no edge function touched) → PASS — `git status --short | grep -E "supabase/functions|config.toml"` returns nothing.
- AC-REG-7 (`app.json`) → PASS — not in the diff; slug untouched.
- AC-REG-8 (no realtime change) → PASS — migration is policy-DDL + one `create or replace function` + comments only; no `ALTER PUBLICATION` statement anywhere in the file (confirmed by reading it in full).

**D. Tests**

- AC-16 (pgTAP — primary track) → PASS — `npm run test:db` → **81/81** files green, including the new 20-arm `super_admin_policy_parity.test.sql` (`plan(20)`, 20/20 assertions). Negative-control discipline verified by reading the suite and the spec's recorded falsification runs: AL-7/AL-8 deliberately use a store-**scoped** row visible to the plain user (not a store-less row) so the arm can't pass for the wrong reason (invisibility vs. policy refusal) — confirmed in the suite's header comment and inline comments. IC-3's pre/post-rename bug is documented and fixed in the current file (`delete … where name in (pre, post)`, asserting on both names) — the file comment states this was caught by a negative-control revert run where IC-3 originally passed trivially when IC-2's rename was denied. LINT-1 correctly scans `pg_policies` predicates only (never `pg_proc`/`pg_get_functiondef`), avoiding false-fails on `auth_can_see_store()`, `auth_is_privileged()`, and the probe's payload emission of `auth_is_admin()`. I did not personally re-run the implementer's revert-based falsification passes (that would require mutating the pgTAP file transiently), but I independently re-applied the full migration a second time and reran the whole 81-file suite as a live idempotency + regression check, which is a different but complementary confirmation that the suite is exercising a real, working fix rather than a vacuously-true one.
- AC-17 (jest) → PASS — `src/screens/__tests__/DBInspectorScreen.test.tsx`, 6 arms covering all four probe shapes from the spec (privileged-jwt, privileged-super-admin with negative string assertions, not-privileged/AC-13 framing, fields-absent/AC-15) plus 2 bonus arms (null/undefined total-function guard, non-boolean `is_privileged` treated as absent). `npx jest` → 202 suites / 2203 tests, all green, matching the count recorded in the spec's "Verification performed" section exactly.
- AC-18 (no shell smoke) → PASS (by design) — no new file under `scripts/smoke-*.sh`; confirmed no service-token/key-dependent surface was added by this diff. Not exercised, correctly.
- AC-19 (typecheck gates) → PASS — `npx tsc --noEmit` clean (exit 0); `npm run typecheck:test` clean (exit 0).

### Test run

```
npm run test:db     → 81/81 DB test file(s) passed (incl. super_admin_policy_parity.test.sql, 20/20 assertions)
npx jest             → Test Suites: 202 passed, 202 total / Tests: 2203 passed, 2203 total
npx tsc --noEmit     → clean, exit 0
npm run typecheck:test → clean, exit 0
```

Additional verification performed independently by this review (beyond re-running the recorded commands):
- Re-applied `supabase/migrations/20260809000000_super_admin_policy_parity.sql` a second time via `psql` directly against `supabase_db_imr-inventory` — succeeded with no errors (idempotency, AC-6), then reran `npm run test:db` — still 81/81.
- Live query: `pg_policies` in `public.*` naming `auth_is_admin` in `qual`/`with_check` → 0 rows (AC-REG-1 / LINT-1 cross-check).
- Live query: `pg_get_functiondef('public.auth_is_admin()')` and `obj_description(...)` → body unchanged, comment present with all four required clauses (AC-5, AC-9).
- Live query: `admin_db_inspector_probe()` function definition contains `is_privileged`, `is_super_admin`, `is_admin`; `prosecdef = true` (AC-11).
- `git diff --stat` against the working tree: exactly the 5 files the spec's "Files changed" section names (`specs/157-super-admin-rls-parity.md`, `supabase/migrations/20260809000000_super_admin_policy_parity.sql`, `supabase/tests/super_admin_policy_parity.test.sql`, `src/screens/DBInspectorScreen.tsx`, `src/screens/__tests__/DBInspectorScreen.test.tsx`). No edge function, `app.json`, `vercel.json`, `CLAUDE.md`, staff subtree, i18n catalog, or `permissive_policy_lint.test.sql` in the diff.

### Notes

- **D-0 prod record (dispatcher-run).** The spec's final section documents that the dispatcher ran D-0's read-only verification queries against prod (`ebwnovzzkwhsdxkpyjka`) via Supabase MCP on 2026-08-09: query (a) → exactly 7 rows matching §3's table (no eighth prod-only policy), query (b, corrected with `p.prokind = 'f'`) → 4 rows, none a gate, gate-only probe → 0 rows, and `audit_log` prod count = 4,485 (R-5 context). This satisfies the D-0 **verification** gate — "D-0 gate: SATISFIED — prod apply may proceed post-review per §10." It does **not** satisfy AC-7, which requires the migration to actually be *applied* to prod and both CI gates re-confirmed green afterward. Those are still outstanding per the spec's own "Still owed" list. Flagging this distinction explicitly so it isn't conflated: D-0 verified the ground truth is safe to apply; nobody has applied it yet.
- **AL-7/AL-8 store-scoped-row requirement.** Confirmed correctly implemented: the suite's header comment explains why a store-less row would make these arms worthless (the plain user would fail the SELECT for the wrong reason), and the fixture uses store `00000000-...-0001` (Towson), on which the seeded plain user (`22222222-...`) holds a real `user_stores` grant. The assertion checks the surviving `detail` value rather than just a row count, so both "invisible" and "wrongly mutated" failure modes are caught by the same assertion — this is the sharpest arm in the suite and it's built correctly.
- **LINT-1 scope discipline.** Confirmed the arm scans `pg_policies.qual`/`with_check` only, never `pg_proc`/`pg_get_functiondef` — this avoids false-failing on `auth_can_see_store()` and `auth_is_privileged()` (which legitimately call `auth_is_admin()` in their bodies) and on `admin_db_inspector_probe()` (which legitimately emits `is_admin` in its payload). This distinction is the one the architect specifically flagged as OQ-6's design constraint, and it was implemented correctly — verified by reading the SQL, not just the comment.
- **Jest banner arms — absence ≠ false.** Verified the classifier's ordering explicitly treats "field absent or non-boolean" as `'unknown'` *before* checking `is_privileged === false`, so a pre-migration probe response (`{is_admin: true}` with no `is_privileged` key) correctly renders neutral rather than either a false green (reading absent `is_privileged` as truthy) or a false alarm (reading `undefined` as `false`). This is exactly the AC-15 deploy-skew protection and it's the arm most likely to regress silently if someone later writes `auth.is_privileged ? ... : ...`; jest arm 4 pins it directly against the exported `classifyAuthBanner`, not against a render, so it will catch a regression without dragging `supabase`/`useStore`/`react-navigation` into jsdom (this matches the R-4 risk the architect flagged and the mitigation chosen).
- **AC-REG-4's seven untouched suites.** All seven are present, green, and confirmed absent from the diff by `git diff --stat`. Read `admin_rpcs_privileged.test.sql`'s existing coverage of `admin_db_inspector_probe()` (not modified by this review, taking the spec's characterization on faith after visually confirming the test file is not in the diff and the suite passed): it calls the RPC via `lives_ok` and does not assert on payload shape, so the additive `auth` keys cannot break it — consistent with the "additive-only" contract in AC-11.
- **Framework discipline.** No new test framework introduced. pgTAP suite lands under `supabase/tests/`, jest suite lands in the `component` jsdom project matched by `src/screens/**/*.test.tsx` per `jest.config.js`, exactly matching Spec 022's three-track convention. No shell smoke needed or added (AC-18), correctly justified rather than silently skipped.
- **Non-blocking process items** (not test-engineer's call, surfacing for the release-coordinator): (1) AC-7's prod apply is still outstanding — this is the single Critical-class gap and matches the spec's own R-1 risk callout; (2) the OQ-5 CLAUDE.md bullet is drafted in the spec for the owner to accept/decline but hasn't been actioned — per CLAUDE.md, agents do not edit CLAUDE.md unilaterally, so this is correctly left for the owner, not a test gap.
