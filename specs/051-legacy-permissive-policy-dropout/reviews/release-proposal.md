# Release proposal — Spec 051 (Legacy permissive policy dropout)

## Verdict
verdict: SHIP_READY
rationale: All four reviewers green at the blocking tier (0 Critical / 0 High across code, security, architect post-impl, and test); the lone Should-fix is a single-cell matrix-narrative drift in spec prose (no code/migration/test change), 22+ ACs PASS with 1 PARTIAL that test-engineer explicitly classified Minor/coverage-only with no evidence of defect, and the full suite is green (jest 182/182, pgTAP 31/31 including the new 13-arm file, tsc clean).

## Findings summary
- code-reviewer: 0 Critical, 1 Should-fix, 3 Nits. Should-fix is a single matrix-table cell at [specs/051-legacy-permissive-policy-dropout.md:657](specs/051-legacy-permissive-policy-dropout.md) — the "cleaner error class (RLS 42501 vs trigger raise)" prediction is factually wrong because Postgres BEFORE-ROW triggers fire before RLS WITH CHECK; arm (11) proves the observed reality is trigger P0001 first. Defense-in-depth is intact (both layers reject). Spec's `## Files changed` at line 1232 already acknowledges the correction inline. Nits: a `do $$ … bool_and(...) … set_config` block in arm (5) at [supabase/tests/legacy_permissive_policy_dropout.test.sql:338-386](supabase/tests/legacy_permissive_policy_dropout.test.sql) lacks a one-line role-scope comment (the pattern is unique in the project's pgTAP corpus); `plan(13)` count is verifiable from header but has no compile-time guard (the pgTAP runner catches the drift at CI regardless); `to public` vs `to authenticated` consistency on the new `user_stores` policies vs. the categories rewrites in the same migration (matches the existing `stores` policy idiom from `20260509000000` — code-reviewer explicitly says "do not change without aligning with the existing `stores` policies").
- security-auditor: 0 Critical / 0 High / 0 Medium / 1 Low (informational). The Low is not a finding — it is a confirmation that the architect's design-narrative note about trigger-vs-RLS error-class ordering is benign because both layers reject and arms (10)+(11) prove the new policy is the structural backstop if the trigger is ever weakened. All eight probe results PASS: all four legacy policies dropped on the right tables, WITH CHECK present on own-row `user_stores`, brand-scoped admin policy uses `auth_can_see_brand(s.brand_id)` (not `auth_can_see_store`), no new wide policies introduced, categories WRITE policies untouched, spec 041 closeout strictly additive, CLAUDE.md addition strictly additive, no GRANT/REVOKE drift.
- backend-architect (post-impl drift review): 0 Critical, 0 Should-fix, 1 Minor (developer-self-flagged Matrix B narrative drift). Architect's verdict: "Not load-bearing for this spec — the spec is shipped and the developer's inline note in §Files changed is sufficient breadcrumb. If a future reviewer cross-references the matrix vs. the test, they hit the inline disclaimer first. A retroactive matrix edit is doc-churn for no reader benefit at this point. **No action required.**" All eight checklist items in "Verified-clean items" PASS: migration filename matches design §0; idempotency shape correct (`drop policy if exists` before every `create policy`); EXISTS subquery references `user_stores.store_id` (not stale `id`); WITH CHECK present on own-row policy; Matrix B vs landed test discrepancy is acceptable; spec 041 closeout in the right section; CLAUDE.md bullet placed strictly additively under "Conventions already in use"; `plan(13)` arithmetic correct (7 stores + 4 user_stores + 2 categories).
- test-engineer: 22+ ACs PASS, 0 FAIL, 1 PARTIAL. The Partial is AC (h) — arm (9) covers INSERT-self under the manager JWT but UPDATE-own and DELETE-own on `user_stores` are not directly asserted. Policy is `FOR ALL` with `using (user_id = auth.uid()) with check (user_id = auth.uid())`, so UPDATE/DELETE behave identically to INSERT by construction; test-engineer rates this Minor / "coverage gap, not evidence of a defect." Full run: `npm run typecheck:test` exit 0, `npm test -- --ci` 17 suites / 182 tests PASS, `bash scripts/test-db.sh` 31/31 files PASS including the new file at 13/13 assertions.

## Recommended next steps (ordered)

Since SHIP_READY:

1. **Apply the one-cell matrix-narrative fix inline pre-commit** (optional cleanup; keeps the commit hygiene-clean and prevents a future reviewer cross-referencing the matrix from re-tripping the drift flag). Exact edit:
   - [specs/051-legacy-permissive-policy-dropout.md:657](specs/051-legacy-permissive-policy-dropout.md) — change the rightmost cell of the "brand-A admin INSERTs grant for brand-A user on brand-B store" row from `Tightened at the policy layer instead of the trigger layer. Same end state, cleaner error class (RLS 42501 vs trigger raise).` to something like `Trigger raises P0001 first (BEFORE ROW, before RLS WITH CHECK per Postgres §38.6). Policy is the structural backstop — if the trigger is ever dropped, the policy still rejects with 42501. Defense-in-depth intact, see pgTAP arm (11).` No migration change, no test change, no code change. Aligns the design table with the observed reality the developer already self-flagged at line 1232 and the test arm comment at lines 544-571.

2. **Commit and deploy.** The migration `20260520010000_legacy_permissive_policy_dropout.sql` lands cleanly after spec 050's `20260520000000` slot. Migration is idempotent (every `create policy` preceded by `drop policy if exists`), zero `alter publication` statements so no realtime resnapshot needed, zero GRANT/REVOKE drift so role state is byte-identical post-migration. Closes the Bobby read leak verified on prod (auth_manage_stores OR-shadow on `public.stores`) AND the sibling latent WRITE leak on `public.user_stores` (any authenticated user could grant cross-user same-brand `user_stores` rows via the OR-arm in the legacy policy).

3. **(Optional, separate spec)** Items flagged for tracking but not blocking this ship:
   - **AC (h) coverage gap follow-up**: Add two pgTAP arms under the manager JWT — UPDATE-own `user_stores` row (e.g. flip a column the policy permits to touch) and DELETE-own `user_stores` row — to lock the `FOR ALL` semantics against a future narrowing to `FOR INSERT`. Test-engineer's recommendation; coverage hardening, not a defect fix. Low-cost to fold into the next spec touching `user_stores` RLS.
   - **Code-reviewer's 3 Nits**: a one-line `-- role remains 'authenticated' (set local) for all ops inside this block` comment above the `do $$` at [supabase/tests/legacy_permissive_policy_dropout.test.sql:338](supabase/tests/legacy_permissive_policy_dropout.test.sql); no change needed for `plan(13)` (CI net catches drift); no change needed for `to public` vs `to authenticated` on the `user_stores` policies (matches the existing `stores` idiom — code-reviewer explicitly says do not align in isolation). All cosmetic.
   - **Spec 052 footgun-linter follow-up** (referenced in the new CLAUDE.md bullet at line 66): a CI check that greps for new `using (auth.uid() IS NOT NULL)` permissive ALL policies and flags them for review. Out of scope for this spec — it is the prevention layer that catches the next OR-shadow before it ships.

## Out of scope for this review
- **Spec 052 OR-shadow linter** — Referenced as the prevention follow-up in the new CLAUDE.md bullet; separate spec.
- **Project-wide audit for additional OR-shadow surfaces** — Spec 051 fixed the four tables enumerated in the audit (`stores`, `user_stores`, `ingredient_categories`, `recipe_categories`). A systematic pass across every table with permissive ALL policies (`pg_policies` query: `permissive = 'PERMISSIVE' AND cmd = 'ALL' AND qual LIKE '%auth.uid() IS NOT NULL%'`) is a separate hardening sweep.
- **`P0001` vs `42501` SQLSTATE convention sweep** — Carried over from Spec 049 and Spec 050 release proposals; still a project-wide pending decision about which errcode class destructive operations should surface. Arm (11)'s P0001 assertion is correct for what executes today; the convention decision is cross-cutting.
- **Realtime publication review** — Migration touches RLS policies only, not the publication membership. The `docker restart supabase_realtime_imr-inventory` gotcha from MEMORY.md does not apply.
- **`app.json` slug change** — Stays `towson-inventory` per CLAUDE.md "DO NOT AUTO-FIX".

## Handoff
next_agent: NONE
prompt: SHIP_READY — 0 Critical / 0 High across all four reviewers; 1 cosmetic Should-fix (single matrix-table cell narrative drift at spec line 657) recommended inline pre-commit; full suite green (typecheck clean, 182/182 jest, 31/31 pgTAP including new 13-arm file); closes Bobby read leak + latent user_stores WRITE leak
payload_paths:
  - specs/051-legacy-permissive-policy-dropout/reviews/release-proposal.md
